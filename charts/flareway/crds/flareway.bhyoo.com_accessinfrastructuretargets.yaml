---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: accessinfrastructuretargets.flareway.bhyoo.com
spec:
  group: flareway.bhyoo.com
  names:
    categories:
    - flareway
    kind: AccessInfrastructureTarget
    listKind: AccessInfrastructureTargetList
    plural: accessinfrastructuretargets
    shortNames:
    - cfait
    singular: accessinfrastructuretarget
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Accepted")].status
      name: Accepted
      type: string
    - jsonPath: .status.targetId
      name: Target
      type: string
    - jsonPath: .spec.hostname
      name: Hostname
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AccessInfrastructureTarget is a Cloudflare infrastructure access
          target.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AccessInfrastructureTargetSpec defines an independently registered
              target.
            properties:
              accountRef:
                description: |-
                  LocalObjectReference contains enough information to let you locate the
                  referenced object inside the same namespace.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              adoption:
                default: {}
                description: AccessInfrastructureTargetAdoptionSpec configures explicit
                  target adoption.
                properties:
                  expect:
                    description: AccessInfrastructureTargetAdoptionExpect declares
                      attributes checked before adoption.
                    properties:
                      hostname:
                        type: string
                    type: object
                  mode:
                    default: None
                    description: AdoptionMode controls explicit adoption of a remote
                      resource.
                    enum:
                    - None
                    - AdoptById
                    type: string
                type: object
              deletionPolicy:
                default: Orphan
                description: DeletionPolicy controls whether a managed remote resource
                  is deleted or orphaned.
                enum:
                - Delete
                - Orphan
                type: string
              externalRef:
                description: AccessInfrastructureTargetExternalReference identifies
                  an existing target.
                properties:
                  targetId:
                    minLength: 1
                    type: string
                required:
                - targetId
                type: object
              hostname:
                description: Hostname is case-insensitive and may contain letters,
                  digits, periods, and dashes.
                maxLength: 255
                minLength: 1
                pattern: ^[A-Za-z0-9](?:[A-Za-z0-9.-]{0,253}[A-Za-z0-9])?$
                type: string
              ip:
                description: AccessInfrastructureTargetIP contains one or both IP
                  families.
                properties:
                  ipv4:
                    description: AccessInfrastructureTargetIPv4Address is an IPv4
                      target address.
                    properties:
                      ipAddr:
                        format: ipv4
                        type: string
                      virtualNetworkId:
                        minLength: 1
                        type: string
                      virtualNetworkRef:
                        description: NamespacedLocalObjectReference identifies a namespaced
                          Flareway resource.
                        properties:
                          name:
                            minLength: 1
                            type: string
                          namespace:
                            description: Namespace defaults to the referencing object's
                              namespace.
                            type: string
                        required:
                        - name
                        type: object
                    required:
                    - ipAddr
                    type: object
                  ipv6:
                    description: AccessInfrastructureTargetIPv6Address is an IPv6
                      target address.
                    properties:
                      ipAddr:
                        format: ipv6
                        type: string
                      virtualNetworkId:
                        minLength: 1
                        type: string
                      virtualNetworkRef:
                        description: NamespacedLocalObjectReference identifies a namespaced
                          Flareway resource.
                        properties:
                          name:
                            minLength: 1
                            type: string
                          namespace:
                            description: Namespace defaults to the referencing object's
                              namespace.
                            type: string
                        required:
                        - name
                        type: object
                    required:
                    - ipAddr
                    type: object
                type: object
                x-kubernetes-validations:
                - message: at least one of ipv4 or ipv6 is required
                  rule: has(self.ipv4) || has(self.ipv6)
                - message: ipv4 virtualNetworkRef and virtualNetworkId are mutually
                    exclusive
                  rule: '!has(self.ipv4) || !has(self.ipv4.virtualNetworkRef) || !has(self.ipv4.virtualNetworkId)'
                - message: ipv6 virtualNetworkRef and virtualNetworkId are mutually
                    exclusive
                  rule: '!has(self.ipv6) || !has(self.ipv6.virtualNetworkRef) || !has(self.ipv6.virtualNetworkId)'
              managementPolicy:
                default: Managed
                description: ManagementPolicy controls whether Flareway mutates a
                  remote resource.
                enum:
                - Managed
                - ObserveOnly
                type: string
            required:
            - accountRef
            - hostname
            - ip
            type: object
            x-kubernetes-validations:
            - message: accountRef.name is required
              rule: has(self.accountRef.name) && size(self.accountRef.name) > 0
            - message: ObserveOnly requires externalRef
              rule: self.managementPolicy != 'ObserveOnly' || has(self.externalRef)
            - message: AdoptById requires externalRef
              rule: self.adoption.mode != 'AdoptById' || has(self.externalRef)
          status:
            description: AccessInfrastructureTargetStatus defines the observed state
              without transport metadata.
            properties:
              appliedAt:
                description: AppliedAt is when AppliedHash was last recorded; nil
                  means never applied.
                format: date-time
                type: string
              appliedHash:
                description: AppliedHash is the desired-state hash recorded after
                  the last successful remote convergence.
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              hostname:
                type: string
              ip:
                description: AccessInfrastructureTargetIPStatus records resolved target
                  addresses.
                properties:
                  ipv4:
                    description: AccessInfrastructureTargetIPAddressStatus records
                      a resolved target address.
                    properties:
                      ipAddr:
                        type: string
                      virtualNetworkId:
                        type: string
                    type: object
                  ipv6:
                    description: AccessInfrastructureTargetIPAddressStatus records
                      a resolved target address.
                    properties:
                      ipAddr:
                        type: string
                      virtualNetworkId:
                        type: string
                    type: object
                type: object
              observedGeneration:
                format: int64
                type: integer
              ownershipVerified:
                type: boolean
              targetId:
                type: string
              wouldApply:
                description: WouldApply is the state Managed mode would write when
                  ObserveOnly detects drift.
                properties:
                  hostname:
                    type: string
                  ip:
                    description: AccessInfrastructureTargetIPStatus records resolved
                      target addresses.
                    properties:
                      ipv4:
                        description: AccessInfrastructureTargetIPAddressStatus records
                          a resolved target address.
                        properties:
                          ipAddr:
                            type: string
                          virtualNetworkId:
                            type: string
                        type: object
                      ipv6:
                        description: AccessInfrastructureTargetIPAddressStatus records
                          a resolved target address.
                        properties:
                          ipAddr:
                            type: string
                          virtualNetworkId:
                            type: string
                        type: object
                    type: object
                required:
                - hostname
                - ip
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
