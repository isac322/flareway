---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: hostnameroutes.flareway.bhyoo.com
spec:
  group: flareway.bhyoo.com
  names:
    categories:
    - flareway
    kind: HostnameRoute
    listKind: HostnameRouteList
    plural: hostnameroutes
    shortNames:
    - hroute
    singular: hostnameroute
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Accepted")].status
      name: Accepted
      type: string
    - jsonPath: .spec.hostname
      name: Hostname
      type: string
    - jsonPath: .status.routeId
      name: Route
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HostnameRoute manages one Cloudflare private hostname route.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HostnameRouteSpec defines a private hostname routed through
              a typed tunnel reference.
            properties:
              accountRef:
                description: |-
                  LocalObjectReference contains enough information to let you locate the
                  referenced object inside the same namespace.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              adoption:
                default: {}
                description: AdoptionSpec configures explicit adoption of an existing
                  remote object.
                properties:
                  expect:
                    description: AdoptionExpect declares remote attributes that must
                      match before adoption.
                    properties:
                      domain:
                        maxLength: 253
                        type: string
                      name:
                        maxLength: 100
                        type: string
                    type: object
                  mode:
                    default: None
                    description: AdoptionMode controls explicit adoption of a remote
                      resource.
                    enum:
                    - None
                    - AdoptById
                    type: string
                type: object
              allowedNamespaces:
                default: {}
                description: AllowedNamespaces limits which namespaces may reference
                  a NetworkRoute or HostnameRoute.
                properties:
                  from:
                    default: Same
                    description: AllowedNamespaceFrom selects namespaces permitted
                      to consume a private route.
                    enum:
                    - Same
                    - All
                    - Selector
                    type: string
                  selector:
                    description: |-
                      A label selector is a label query over a set of resources. The result of matchLabels and
                      matchExpressions are ANDed. An empty label selector matches all objects. A null
                      label selector matches no objects.
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
                x-kubernetes-validations:
                - message: selector is required when from is Selector
                  rule: '!has(self.from) || self.from != ''Selector'' || has(self.selector)'
                - message: selector is only valid when from is Selector
                  rule: '!has(self.selector) || (has(self.from) && self.from == ''Selector'')'
              comment:
                maxLength: 256
                type: string
              deletionPolicy:
                default: Orphan
                description: DeletionPolicy controls whether a managed remote resource
                  is deleted or orphaned.
                enum:
                - Delete
                - Orphan
                type: string
              externalRef:
                description: HostnameRouteExternalReference identifies an existing
                  Cloudflare hostname route.
                properties:
                  routeId:
                    minLength: 1
                    type: string
                required:
                - routeId
                type: object
              hostname:
                maxLength: 253
                pattern: ^(?:\*\.)?(?:[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?\.)+[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?$
                type: string
              managementPolicy:
                default: Managed
                description: ManagementPolicy controls whether Flareway mutates a
                  remote resource.
                enum:
                - Managed
                - ObserveOnly
                type: string
              tunnelRef:
                description: TunnelReference identifies a namespaced CloudflareTunnel
                  or WARPConnector.
                properties:
                  kind:
                    default: CloudflareTunnel
                    description: TunnelReferenceKind selects a supported private-route
                      tunnel resource.
                    enum:
                    - CloudflareTunnel
                    - WARPConnector
                    type: string
                  name:
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace defaults to the referencing object's namespace.
                    type: string
                required:
                - name
                type: object
            required:
            - accountRef
            - hostname
            - tunnelRef
            type: object
            x-kubernetes-validations:
            - message: ObserveOnly requires externalRef
              rule: self.managementPolicy != 'ObserveOnly' || has(self.externalRef)
            - message: AdoptById requires externalRef
              rule: self.adoption.mode != 'AdoptById' || has(self.externalRef)
            - message: Managed externalRef requires adoption.mode AdoptById
              rule: '!has(self.externalRef) || self.managementPolicy == ''ObserveOnly''
                || self.adoption.mode == ''AdoptById'''
            - message: accountRef.name is required
              rule: has(self.accountRef.name) && size(self.accountRef.name) > 0
            - message: tunnelRef.name is required
              rule: has(self.tunnelRef.name) && size(self.tunnelRef.name) > 0
          status:
            description: HostnameRouteStatus records actionable hostname-route and
              lookup metadata.
            properties:
              applied:
                description: HostnameRouteAppliedStatus is the exact hostname and
                  resolved remote tunnel last applied.
                properties:
                  hostname:
                    type: string
                  observedGeneration:
                    format: int64
                    type: integer
                  tunnelId:
                    type: string
                  tunnelType:
                    description: TunnelRemoteType is the public representation of
                      a Cloudflare tunnel type.
                    enum:
                    - CloudflareTunnel
                    - WARPConnector
                    - WARP
                    - Magic
                    - IPSec
                    - GRE
                    - CNI
                    type: string
                type: object
              comment:
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 16
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              createdAt:
                format: date-time
                type: string
              deletedAt:
                format: date-time
                type: string
              hostname:
                type: string
              observedGeneration:
                format: int64
                type: integer
              ownershipVerified:
                type: boolean
              routeId:
                type: string
              tunnelId:
                type: string
              tunnelName:
                type: string
              tunnelType:
                description: TunnelRemoteType is the public representation of a Cloudflare
                  tunnel type.
                enum:
                - CloudflareTunnel
                - WARPConnector
                - WARP
                - Magic
                - IPSec
                - GRE
                - CNI
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
