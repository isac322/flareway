---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: cloudflaretunnels.flareway.bhyoo.com
spec:
  group: flareway.bhyoo.com
  names:
    categories:
    - flareway
    kind: CloudflareTunnel
    listKind: CloudflareTunnelList
    plural: cloudflaretunnels
    shortNames:
    - cft
    singular: cloudflaretunnel
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.tunnelId
      name: Tunnel
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.gatewayRef.name
      name: Gateway
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CloudflareTunnel represents one remotely managed Cloudflare Tunnel
          and its Gateway data plane.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CloudflareTunnelSpec defines remote ownership and connector
              configuration.
            properties:
              accountRef:
                description: |-
                  LocalObjectReference contains enough information to let you locate the
                  referenced object inside the same namespace.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              adoption:
                default: {}
                description: AdoptionSpec configures explicit adoption of an existing
                  remote object.
                properties:
                  expect:
                    description: AdoptionExpect declares remote attributes that must
                      match before adoption.
                    properties:
                      domain:
                        maxLength: 253
                        type: string
                      name:
                        maxLength: 100
                        type: string
                    type: object
                  mode:
                    default: None
                    description: AdoptionMode controls explicit adoption of a remote
                      resource.
                    enum:
                    - None
                    - AdoptById
                    type: string
                type: object
              configuration:
                default: {}
                description: CloudflareTunnelConfiguration selects the single configuration
                  writer for a tunnel.
                properties:
                  direct:
                    description: CloudflareTunnelDirectConfiguration is the whole-object
                      configuration owned by Direct mode.
                    properties:
                      ingress:
                        items:
                          description: CloudflareTunnelIngressRule is one ordered
                            direct-mode ingress rule.
                          properties:
                            hostname:
                              description: Hostname may be omitted only for the final
                                catch-all rule.
                              maxLength: 253
                              type: string
                            originRequest:
                              description: CloudflareTunnelOriginRequest is the complete
                                remotely configurable cloudflared origin policy.
                              properties:
                                access:
                                  description: CloudflareTunnelOriginAccess configures
                                    Access JWT validation at cloudflared.
                                  properties:
                                    audTags:
                                      items:
                                        maxLength: 256
                                        minLength: 1
                                        type: string
                                      maxItems: 64
                                      type: array
                                      x-kubernetes-list-type: set
                                    required:
                                      type: boolean
                                    teamName:
                                      maxLength: 63
                                      minLength: 1
                                      type: string
                                  required:
                                  - audTags
                                  - teamName
                                  type: object
                                  x-kubernetes-validations:
                                  - message: teamName is required
                                    rule: has(self.teamName) && size(self.teamName)
                                      > 0
                                  - message: at least one audTag is required
                                    rule: size(self.audTags) > 0
                                caPool:
                                  type: string
                                connectTimeout:
                                  format: int64
                                  minimum: 0
                                  type: integer
                                disableChunkedEncoding:
                                  type: boolean
                                http2Origin:
                                  type: boolean
                                httpHostHeader:
                                  type: string
                                ipRules:
                                  items:
                                    description: CloudflareTunnelIPRule configures
                                      one bastion or SOCKS5 source-IP rule.
                                    properties:
                                      allow:
                                        type: boolean
                                      ports:
                                        items:
                                          format: int32
                                          maximum: 65535
                                          minimum: 1
                                          type: integer
                                        maxItems: 64
                                        type: array
                                        x-kubernetes-list-type: set
                                      prefix:
                                        maxLength: 49
                                        minLength: 3
                                        type: string
                                        x-kubernetes-validations:
                                        - message: prefix must be a valid IPv4 or
                                            IPv6 CIDR
                                          rule: isCIDR(self)
                                    required:
                                    - allow
                                    - prefix
                                    type: object
                                  maxItems: 64
                                  type: array
                                  x-kubernetes-list-type: atomic
                                keepAliveConnections:
                                  format: int64
                                  minimum: 0
                                  type: integer
                                keepAliveTimeout:
                                  format: int64
                                  minimum: 0
                                  type: integer
                                matchSNIToHost:
                                  type: boolean
                                noHappyEyeballs:
                                  type: boolean
                                noTLSVerify:
                                  type: boolean
                                originServerName:
                                  type: string
                                proxyType:
                                  description: CloudflareTunnelProxyType selects stream
                                    proxy translation behavior.
                                  enum:
                                  - Regular
                                  - SOCKS5
                                  type: string
                                tcpKeepAlive:
                                  format: int64
                                  minimum: 0
                                  type: integer
                                tlsTimeout:
                                  format: int64
                                  minimum: 0
                                  type: integer
                              type: object
                            path:
                              description: Path is a Go regular expression evaluated
                                by cloudflared.
                              maxLength: 2048
                              type: string
                            service:
                              description: CloudflareTunnelIngressService selects
                                exactly one official cloudflared ingress service form.
                              properties:
                                bastion:
                                  description: CloudflareTunnelBuiltinService is a
                                    marker for a parameterless cloudflared service.
                                  type: object
                                helloWorld:
                                  description: CloudflareTunnelBuiltinService is a
                                    marker for a parameterless cloudflared service.
                                  type: object
                                http:
                                  description: CloudflareTunnelAddressService identifies
                                    a network origin without embedding its wire protocol.
                                  properties:
                                    address:
                                      description: Address is a host:port pair accepted
                                        by cloudflared.
                                      maxLength: 2048
                                      minLength: 1
                                      type: string
                                  required:
                                  - address
                                  type: object
                                httpStatus:
                                  description: CloudflareTunnelHTTPStatusService configures
                                    the embedded HTTP status responder.
                                  properties:
                                    code:
                                      format: int32
                                      maximum: 599
                                      minimum: 100
                                      type: integer
                                  required:
                                  - code
                                  type: object
                                https:
                                  description: CloudflareTunnelAddressService identifies
                                    a network origin without embedding its wire protocol.
                                  properties:
                                    address:
                                      description: Address is a host:port pair accepted
                                        by cloudflared.
                                      maxLength: 2048
                                      minLength: 1
                                      type: string
                                  required:
                                  - address
                                  type: object
                                rdp:
                                  description: CloudflareTunnelAddressService identifies
                                    a network origin without embedding its wire protocol.
                                  properties:
                                    address:
                                      description: Address is a host:port pair accepted
                                        by cloudflared.
                                      maxLength: 2048
                                      minLength: 1
                                      type: string
                                  required:
                                  - address
                                  type: object
                                smb:
                                  description: CloudflareTunnelAddressService identifies
                                    a network origin without embedding its wire protocol.
                                  properties:
                                    address:
                                      description: Address is a host:port pair accepted
                                        by cloudflared.
                                      maxLength: 2048
                                      minLength: 1
                                      type: string
                                  required:
                                  - address
                                  type: object
                                ssh:
                                  description: CloudflareTunnelAddressService identifies
                                    a network origin without embedding its wire protocol.
                                  properties:
                                    address:
                                      description: Address is a host:port pair accepted
                                        by cloudflared.
                                      maxLength: 2048
                                      minLength: 1
                                      type: string
                                  required:
                                  - address
                                  type: object
                                tcp:
                                  description: CloudflareTunnelAddressService identifies
                                    a network origin without embedding its wire protocol.
                                  properties:
                                    address:
                                      description: Address is a host:port pair accepted
                                        by cloudflared.
                                      maxLength: 2048
                                      minLength: 1
                                      type: string
                                  required:
                                  - address
                                  type: object
                                unix:
                                  description: CloudflareTunnelUnixService identifies
                                    a UNIX domain socket origin.
                                  properties:
                                    path:
                                      maxLength: 4096
                                      minLength: 1
                                      type: string
                                  required:
                                  - path
                                  type: object
                                unixTLS:
                                  description: CloudflareTunnelUnixService identifies
                                    a UNIX domain socket origin.
                                  properties:
                                    path:
                                      maxLength: 4096
                                      minLength: 1
                                      type: string
                                  required:
                                  - path
                                  type: object
                              type: object
                              x-kubernetes-validations:
                              - message: exactly one ingress service must be configured
                                rule: (has(self.http)?1:0)+(has(self.https)?1:0)+(has(self.tcp)?1:0)+(has(self.ssh)?1:0)+(has(self.rdp)?1:0)+(has(self.smb)?1:0)+(has(self.unix)?1:0)+(has(self.unixTLS)?1:0)+(has(self.helloWorld)?1:0)+(has(self.httpStatus)?1:0)+(has(self.bastion)?1:0)
                                  == 1
                          required:
                          - service
                          type: object
                          x-kubernetes-validations:
                          - message: originRequest.access is only valid for HTTP origins
                            rule: '!has(self.originRequest) || !has(self.originRequest.access)
                              || has(self.service.http) || has(self.service.https)
                              || has(self.service.unix) || has(self.service.unixTLS)'
                          - message: originRequest.ipRules requires Bastion or SOCKS5
                              proxy service
                            rule: '!has(self.originRequest) || size(self.originRequest.ipRules)
                              == 0 || has(self.service.bastion) || (has(self.originRequest.proxyType)
                              && self.originRequest.proxyType == ''SOCKS5'')'
                        maxItems: 200
                        minItems: 1
                        type: array
                        x-kubernetes-list-type: atomic
                      originRequest:
                        description: CloudflareTunnelOriginRequest is the complete
                          remotely configurable cloudflared origin policy.
                        properties:
                          access:
                            description: CloudflareTunnelOriginAccess configures Access
                              JWT validation at cloudflared.
                            properties:
                              audTags:
                                items:
                                  maxLength: 256
                                  minLength: 1
                                  type: string
                                maxItems: 64
                                type: array
                                x-kubernetes-list-type: set
                              required:
                                type: boolean
                              teamName:
                                maxLength: 63
                                minLength: 1
                                type: string
                            required:
                            - audTags
                            - teamName
                            type: object
                            x-kubernetes-validations:
                            - message: teamName is required
                              rule: has(self.teamName) && size(self.teamName) > 0
                            - message: at least one audTag is required
                              rule: size(self.audTags) > 0
                          caPool:
                            type: string
                          connectTimeout:
                            format: int64
                            minimum: 0
                            type: integer
                          disableChunkedEncoding:
                            type: boolean
                          http2Origin:
                            type: boolean
                          httpHostHeader:
                            type: string
                          ipRules:
                            items:
                              description: CloudflareTunnelIPRule configures one bastion
                                or SOCKS5 source-IP rule.
                              properties:
                                allow:
                                  type: boolean
                                ports:
                                  items:
                                    format: int32
                                    maximum: 65535
                                    minimum: 1
                                    type: integer
                                  maxItems: 64
                                  type: array
                                  x-kubernetes-list-type: set
                                prefix:
                                  maxLength: 49
                                  minLength: 3
                                  type: string
                                  x-kubernetes-validations:
                                  - message: prefix must be a valid IPv4 or IPv6 CIDR
                                    rule: isCIDR(self)
                              required:
                              - allow
                              - prefix
                              type: object
                            maxItems: 64
                            type: array
                            x-kubernetes-list-type: atomic
                          keepAliveConnections:
                            format: int64
                            minimum: 0
                            type: integer
                          keepAliveTimeout:
                            format: int64
                            minimum: 0
                            type: integer
                          matchSNIToHost:
                            type: boolean
                          noHappyEyeballs:
                            type: boolean
                          noTLSVerify:
                            type: boolean
                          originServerName:
                            type: string
                          proxyType:
                            description: CloudflareTunnelProxyType selects stream
                              proxy translation behavior.
                            enum:
                            - Regular
                            - SOCKS5
                            type: string
                          tcpKeepAlive:
                            format: int64
                            minimum: 0
                            type: integer
                          tlsTimeout:
                            format: int64
                            minimum: 0
                            type: integer
                        type: object
                      warpRouting:
                        description: CloudflareTunnelWARPRouting configures private-network
                          origin dialing.
                        properties:
                          connectTimeout:
                            format: int64
                            minimum: 0
                            type: integer
                          enabled:
                            type: boolean
                          maxActiveFlows:
                            format: int64
                            minimum: 0
                            type: integer
                          tcpKeepAlive:
                            format: int64
                            minimum: 0
                            type: integer
                        type: object
                    required:
                    - ingress
                    type: object
                    x-kubernetes-validations:
                    - message: the final ingress rule must be a catch-all
                      rule: (!has(self.ingress[size(self.ingress)-1].hostname) ||
                        size(self.ingress[size(self.ingress)-1].hostname) == 0) &&
                        (!has(self.ingress[size(self.ingress)-1].path) || size(self.ingress[size(self.ingress)-1].path)
                        == 0)
                  mode:
                    default: Gateway
                    description: CloudflareTunnelConfigurationMode selects Gateway
                      aggregation or direct Cloudflare configuration.
                    enum:
                    - Gateway
                    - Direct
                    type: string
                type: object
                x-kubernetes-validations:
                - message: Direct mode requires direct configuration
                  rule: '!has(self.mode) || self.mode != ''Direct'' || has(self.direct)'
                - message: direct configuration is only valid in Direct mode
                  rule: '!has(self.direct) || (has(self.mode) && self.mode == ''Direct'')'
              connector:
                description: Gateway-mode overrides.
                properties:
                  gracePeriod:
                    default: 60s
                    type: string
                  image:
                    default: cloudflare/cloudflared:2026.9.1@sha256:b269e8abd07a5bf6f3f4be65d5050b2174eca89c56a0241a8ff32a16aec454e4
                    type: string
                  protocol:
                    default: Auto
                    description: ConnectorProtocol selects the transport protocol
                      used by cloudflared.
                    enum:
                    - Auto
                    - QUIC
                    - HTTP2
                    type: string
                  replicas:
                    default: 2
                    format: int32
                    maximum: 25
                    minimum: 1
                    type: integer
                  resources:
                    description: ResourceRequirements describes the compute resource
                      requirements.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                type: object
              deletionPolicy:
                default: Delete
                description: DeletionPolicy controls whether a managed remote resource
                  is deleted or orphaned.
                enum:
                - Delete
                - Orphan
                type: string
              dns:
                default: {}
                description: DNS applies to hostnames owned by either configuration
                  mode.
                properties:
                  mode:
                    default: Managed
                    description: DNSMode controls whether Flareway manages public
                      DNS records.
                    enum:
                    - Managed
                    - External
                    type: string
                  proxied:
                    default: true
                    type: boolean
                  recordComment:
                    default: managed-by=flareway
                    maxLength: 100
                    type: string
                  settings:
                    description: DNSRecordSettings configures Cloudflare DNS record
                      IP-family behavior.
                    properties:
                      ipv4Only:
                        type: boolean
                      ipv6Only:
                        type: boolean
                    type: object
                    x-kubernetes-validations:
                    - message: ipv4Only and ipv6Only cannot both be true
                      rule: '!(has(self.ipv4Only) && self.ipv4Only && has(self.ipv6Only)
                        && self.ipv6Only)'
                  ttl:
                    format: int64
                    type: integer
                type: object
                x-kubernetes-validations:
                - message: ttl must be 1 (automatic) or between 60 and 86400 seconds
                  rule: '!has(self.ttl) || self.ttl == 1 || (self.ttl >= 60 && self.ttl
                    <= 86400)'
                - message: proxied DNS records require automatic ttl
                  rule: '!has(self.proxied) || !self.proxied || !has(self.ttl) ||
                    self.ttl == 1'
                - message: ipv4Only or ipv6Only requires proxied=true
                  rule: '!has(self.settings) || ((!has(self.settings.ipv4Only) ||
                    !self.settings.ipv4Only) && (!has(self.settings.ipv6Only) || !self.settings.ipv6Only))
                    || (has(self.proxied) && self.proxied)'
              listeners:
                items:
                  description: CloudflareTunnelListener configures exposure for one
                    Gateway listener.
                  properties:
                    exposure:
                      default: Public
                      description: Exposure selects a Cloudflare public-edge or private-WARP
                        exposure.
                      enum:
                      - Public
                      - Private
                      type: string
                    hostnameRoute:
                      default: {}
                      description: CloudflareTunnelHostnameRouteConfig configures
                        automatic HostnameRoute creation.
                      properties:
                        create:
                          default: true
                          type: boolean
                      type: object
                    name:
                      description: |-
                        SectionName is the name of a section in a Kubernetes resource.

                        In the following resources, SectionName is interpreted as the following:

                        * Gateway: Listener name
                        * HTTPRoute: HTTPRouteRule name
                        * Service: Port name

                        Section names can have a variety of forms, including RFC 1123 subdomains,
                        RFC 1123 labels, or RFC 1035 labels.

                        This validation is based off of the corresponding Kubernetes validation:
                        https://github.com/kubernetes/apimachinery/blob/02cfb53916346d085a6c6c7c66f882e3c6b0eca6/pkg/util/validation/validation.go#L208

                        Valid values include:

                        * "example"
                        * "foo-example"
                        * "example.com"
                        * "foo.example.com"

                        Invalid values include:

                        * "example.com/bar" - "/" is an invalid character
                      maxLength: 253
                      minLength: 1
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                      type: string
                    virtualNetworkRef:
                      description: |-
                        LocalObjectReference contains enough information to let you locate the
                        referenced object inside the same namespace.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                  required:
                  - name
                  type: object
                maxItems: 64
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              managementPolicy:
                default: Managed
                description: ManagementPolicy controls whether Flareway mutates a
                  remote resource.
                enum:
                - Managed
                - ObserveOnly
                type: string
              managementToken:
                description: CloudflareTunnelManagementTokenRequest requests an owned
                  Secret containing a short-lived token.
                properties:
                  resources:
                    items:
                      description: CloudflareTunnelManagementResource selects a short-lived
                        management-token capability.
                      enum:
                      - Logs
                      type: string
                    maxItems: 1
                    minItems: 1
                    type: array
                    x-kubernetes-list-type: set
                required:
                - resources
                type: object
              originRequest:
                description: GatewayOriginRequestSpec contains only origin settings
                  valid for cloudflared's loopback Envoy origin.
                properties:
                  connectTimeout:
                    type: string
                  disableChunkedEncoding:
                    type: boolean
                  http2Origin:
                    type: boolean
                  keepAliveConnections:
                    format: int64
                    minimum: 0
                    type: integer
                  keepAliveTimeout:
                    type: string
                  noHappyEyeballs:
                    type: boolean
                  tcpKeepAlive:
                    type: string
                type: object
              privateDNS:
                description: PrivateDNSSpec configures the CoreDNS sidecar used by
                  private listeners.
                properties:
                  image:
                    default: coredns/coredns:1.14.7@sha256:7efd3c635b03efd68c4e8398fc45f0d993d0e9ab016f72c1cefb0fd6d01aa286
                    type: string
                  resources:
                    description: ResourceRequirements describes the compute resource
                      requirements.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                type: object
              proxy:
                description: ProxySpec configures the Envoy proxy containers.
                properties:
                  concurrency:
                    default: 1
                    format: int32
                    minimum: 1
                    type: integer
                  image:
                    default: envoyproxy/envoy:distroless-v1.39.1
                    type: string
                  resources:
                    description: ResourceRequirements describes the compute resource
                      requirements.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  streamIdleTimeout:
                    default: 1h
                    type: string
                type: object
              tunnel:
                default: {}
                description: CloudflareTunnelRemoteSpec configures the remote tunnel
                  identity.
                properties:
                  externalRef:
                    description: CloudflareTunnelExternalReference identifies an existing
                      Cloudflare Tunnel.
                    properties:
                      tunnelId:
                        minLength: 1
                        type: string
                    required:
                    - tunnelId
                    type: object
                  name:
                    description: Name defaults in the controller to "<cluster>-<namespace>-<name>".
                    maxLength: 100
                    type: string
                type: object
            required:
            - accountRef
            type: object
            x-kubernetes-validations:
            - message: ObserveOnly requires tunnel.externalRef
              rule: self.managementPolicy != 'ObserveOnly' || has(self.tunnel.externalRef)
            - message: AdoptById requires tunnel.externalRef
              rule: self.adoption.mode != 'AdoptById' || has(self.tunnel.externalRef)
            - message: AdoptById requires adoption.expect.name
              rule: self.adoption.mode != 'AdoptById' || (has(self.adoption.expect)
                && has(self.adoption.expect.name) && size(self.adoption.expect.name)
                > 0)
            - message: Managed externalRef requires adoption.mode AdoptById
              rule: '!has(self.tunnel.externalRef) || self.managementPolicy == ''ObserveOnly''
                || self.adoption.mode == ''AdoptById'''
            - message: accountRef.name is required
              rule: has(self.accountRef.name) && size(self.accountRef.name) > 0
            - message: Direct mode cannot use Gateway connector, proxy, privateDNS,
                originRequest, or listeners
              rule: '!has(self.configuration.mode) || self.configuration.mode != ''Direct''
                || (!has(self.connector) && !has(self.proxy) && !has(self.privateDNS)
                && !has(self.originRequest) && (!has(self.listeners) || size(self.listeners)
                == 0))'
          status:
            description: CloudflareTunnelStatus contains bounded remote observations
              and disjoint controller-owned fields.
            properties:
              accountId:
                type: string
              addresses:
                items:
                  description: GatewayStatusAddress describes a network address that
                    is bound to a Gateway.
                  properties:
                    type:
                      default: IPAddress
                      description: Type of the address.
                      maxLength: 253
                      minLength: 1
                      pattern: ^Hostname|IPAddress|NamedAddress|[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*\/[A-Za-z0-9\/\-._~%!$&'()*+,;=:]+$
                      type: string
                    value:
                      description: |-
                        Value of the address. The validity of the values will depend
                        on the type and support by the controller.

                        Examples: `1.2.3.4`, `128::1`, `my-ip-address`.
                      maxLength: 253
                      minLength: 1
                      type: string
                  required:
                  - value
                  type: object
                  x-kubernetes-validations:
                  - message: Hostname value must only contain valid characters (matching
                      ^(\*\.)?[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$)
                    rule: 'self.type == ''Hostname'' ? self.value.matches(r"""^(\*\.)?[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$"""):
                      true'
                maxItems: 16
                type: array
                x-kubernetes-list-type: atomic
              clients:
                items:
                  description: CloudflareTunnelClientStatus records one bounded cloudflared
                    client observation.
                  properties:
                    arch:
                      type: string
                    configVersion:
                      format: int64
                      type: integer
                    connections:
                      items:
                        description: CloudflareTunnelConnectionStatus records one
                          edge connection.
                        properties:
                          clientId:
                            type: string
                          clientVersion:
                            type: string
                          coloName:
                            type: string
                          id:
                            minLength: 1
                            type: string
                          openedAt:
                            format: date-time
                            type: string
                          originIp:
                            type: string
                          uuid:
                            type: string
                        required:
                        - id
                        type: object
                      maxItems: 16
                      type: array
                      x-kubernetes-list-map-keys:
                      - id
                      x-kubernetes-list-type: map
                    features:
                      items:
                        maxLength: 128
                        type: string
                      maxItems: 64
                      type: array
                      x-kubernetes-list-type: set
                    id:
                      type: string
                    runAt:
                      format: date-time
                      type: string
                    version:
                      type: string
                  required:
                  - id
                  type: object
                maxItems: 25
                type: array
                x-kubernetes-list-map-keys:
                - id
                x-kubernetes-list-type: map
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 16
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              configSource:
                description: TunnelConfigSource is the public representation of tunnel
                  configuration ownership.
                enum:
                - Local
                - Cloudflare
                type: string
              configVersion:
                description: CloudflareTunnelConfigVersion records desired and observed
                  whole-object configuration versions.
                properties:
                  applied:
                    format: int64
                    type: integer
                  createdAt:
                    format: date-time
                    type: string
                  desired:
                    format: int64
                    type: integer
                  desiredHash:
                    type: string
                  remote:
                    format: int64
                    type: integer
                type: object
              connectionsActiveAt:
                format: date-time
                type: string
              connectionsInactiveAt:
                format: date-time
                type: string
              connectorState:
                description: ConnectorState is the public representation of observed
                  tunnel health.
                enum:
                - Healthy
                - Degraded
                - Down
                - Inactive
                type: string
              connectorTokenSecretRef:
                description: |-
                  LocalObjectReference contains enough information to let you locate the
                  referenced object inside the same namespace.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              createdAt:
                format: date-time
                type: string
              deletedAt:
                format: date-time
                type: string
              dnsRecords:
                items:
                  description: CloudflareTunnelDNSRecordStatus records a bounded projection
                    of one managed DNS record.
                  properties:
                    commentModifiedOn:
                      format: date-time
                      type: string
                    createdOn:
                      format: date-time
                      type: string
                    hostname:
                      type: string
                    modifiedOn:
                      format: date-time
                      type: string
                    ownershipComment:
                      description: OwnershipComment checkpoints the exact comment
                        written when this record was owned.
                      maxLength: 100
                      type: string
                    proxiable:
                      type: boolean
                    proxied:
                      type: boolean
                    recordId:
                      type: string
                    settings:
                      description: DNSRecordSettings configures Cloudflare DNS record
                        IP-family behavior.
                      properties:
                        ipv4Only:
                          type: boolean
                        ipv6Only:
                          type: boolean
                      type: object
                      x-kubernetes-validations:
                      - message: ipv4Only and ipv6Only cannot both be true
                        rule: '!(has(self.ipv4Only) && self.ipv4Only && has(self.ipv6Only)
                          && self.ipv6Only)'
                    state:
                      type: string
                    ttl:
                      format: int64
                      type: integer
                    zoneId:
                      type: string
                  required:
                  - hostname
                  - recordId
                  - zoneId
                  type: object
                maxItems: 256
                type: array
                x-kubernetes-list-map-keys:
                - hostname
                x-kubernetes-list-type: map
              gatewayRef:
                description: GatewayRef names the Gateway selected by the Tunnel controller.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              gatewayUid:
                description: GatewayUID binds GatewayRef to one exact live Kubernetes
                  object.
                type: string
              hostnames:
                items:
                  description: CloudflareTunnelHostnameStatus is owned by the gateway
                    field manager.
                  properties:
                    accessApplication:
                      type: string
                    appliedVersion:
                      format: int64
                      type: integer
                    guard:
                      description: HostnameGuard records the fail-closed state applied
                        to a public hostname.
                      enum:
                      - Forwarding
                      - Blocked
                      - Unprotected
                      type: string
                    hostname:
                      type: string
                    protectionDomain:
                      type: string
                  required:
                  - accessApplication
                  - guard
                  - hostname
                  - protectionDomain
                  type: object
                maxItems: 256
                type: array
                x-kubernetes-list-map-keys:
                - hostname
                - protectionDomain
                - accessApplication
                x-kubernetes-list-type: map
              listeners:
                items:
                  description: CloudflareTunnelListenerStatus is owned by the gateway
                    field manager.
                  properties:
                    binding:
                      description: ListenerBinding identifies the private Envoy listener
                        binding strategy.
                      enum:
                      - Loopback
                      - PodIP
                      type: string
                    exposure:
                      description: Exposure selects a Cloudflare public-edge or private-WARP
                        exposure.
                      enum:
                      - Public
                      - Private
                      type: string
                    name:
                      description: |-
                        SectionName is the name of a section in a Kubernetes resource.

                        In the following resources, SectionName is interpreted as the following:

                        * Gateway: Listener name
                        * HTTPRoute: HTTPRouteRule name
                        * Service: Port name

                        Section names can have a variety of forms, including RFC 1123 subdomains,
                        RFC 1123 labels, or RFC 1035 labels.

                        This validation is based off of the corresponding Kubernetes validation:
                        https://github.com/kubernetes/apimachinery/blob/02cfb53916346d085a6c6c7c66f882e3c6b0eca6/pkg/util/validation/validation.go#L208

                        Valid values include:

                        * "example"
                        * "foo-example"
                        * "example.com"
                        * "foo.example.com"

                        Invalid values include:

                        * "example.com/bar" - "/" is an invalid character
                      maxLength: 253
                      minLength: 1
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                      type: string
                    protectionDomains:
                      items:
                        description: CloudflareProtectionDomainStatus records one
                          Envoy protection domain.
                        properties:
                          envoyPort:
                            format: int32
                            type: integer
                          name:
                            type: string
                          protected:
                            type: boolean
                        required:
                        - envoyPort
                        - name
                        - protected
                        type: object
                      maxItems: 64
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                  required:
                  - exposure
                  - name
                  type: object
                maxItems: 64
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              managementTokenSecretRef:
                description: |-
                  LocalObjectReference contains enough information to let you locate the
                  referenced object inside the same namespace.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              name:
                type: string
              observedGeneration:
                format: int64
                type: integer
              orphanedTunnelId:
                type: string
              ownershipVerified:
                type: boolean
              tunnelId:
                type: string
              tunnelType:
                description: TunnelRemoteType is the public representation of a Cloudflare
                  tunnel type.
                enum:
                - CloudflareTunnel
                - WARPConnector
                - WARP
                - Magic
                - IPSec
                - GRE
                - CNI
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
