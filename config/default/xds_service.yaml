apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: controller-manager
    app.kubernetes.io/name: flareway
    app.kubernetes.io/managed-by: kustomize
  name: xds
  namespace: system
spec:
  ports:
  - name: xds
    port: 18000
    protocol: TCP
    targetPort: 18000
  selector:
    control-plane: controller-manager
    app.kubernetes.io/name: flareway
  type: ClusterIP
