---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: accesspolicies.flareway.bhyoo.com
spec:
  group: flareway.bhyoo.com
  names:
    categories:
    - flareway
    kind: AccessPolicy
    listKind: AccessPolicyList
    plural: accesspolicies
    shortNames:
    - cfap
    singular: accesspolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Accepted")].status
      name: Accepted
      type: string
    - jsonPath: .status.policyId
      name: Policy
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AccessPolicy manages a reusable Cloudflare Access policy.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AccessPolicySpec defines a reusable account-level Access
              policy.
            properties:
              accountRef:
                description: |-
                  LocalObjectReference contains enough information to let you locate the
                  referenced object inside the same namespace.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              adoption:
                default: {}
                description: AdoptionSpec configures explicit adoption of an existing
                  remote object.
                properties:
                  expect:
                    description: AdoptionExpect declares remote attributes that must
                      match before adoption.
                    properties:
                      domain:
                        type: string
                      name:
                        type: string
                    type: object
                  mode:
                    default: None
                    description: AdoptionMode controls explicit adoption of a remote
                      resource.
                    enum:
                    - None
                    - AdoptById
                    type: string
                type: object
              approval:
                description: AccessPolicyApproval configures policy approval requirements.
                properties:
                  groups:
                    items:
                      description: AccessPolicyApprovalGroup configures an approval
                        group.
                      properties:
                        approvalsNeeded:
                          format: int32
                          minimum: 1
                          type: integer
                        emailAddresses:
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        emailListId:
                          type: string
                      required:
                      - approvalsNeeded
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  required:
                    type: boolean
                type: object
              decision:
                description: AccessPolicyDecision is the action taken for a matching
                  Access policy.
                enum:
                - allow
                - deny
                - nonIdentity
                - bypass
                type: string
              deletionPolicy:
                default: Orphan
                description: DeletionPolicy controls whether a managed remote resource
                  is deleted or orphaned.
                enum:
                - Delete
                - Orphan
                type: string
              exclude:
                items:
                  description: AccessRule is a typed union of every reusable Access
                    rule supported by Cloudflare.
                  maxProperties: 1
                  minProperties: 1
                  properties:
                    anyValidServiceToken:
                      description: AccessAnyValidServiceTokenRule is part of the accessanyvalidservicetokenrule
                        configuration.
                      type: object
                    authContext:
                      description: AccessAuthContextRule is part of the accessauthcontextrule
                        configuration.
                      properties:
                        acId:
                          type: string
                        id:
                          type: string
                        identityProviderRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      required:
                      - acId
                      - id
                      - identityProviderRef
                      type: object
                    authMethod:
                      description: AccessAuthMethodRule is part of the accessauthmethodrule
                        configuration.
                      properties:
                        authMethod:
                          type: string
                      required:
                      - authMethod
                      type: object
                    azureAD:
                      description: AccessAzureADRule is part of the accessazureadrule
                        configuration.
                      properties:
                        id:
                          type: string
                        identityProviderRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      required:
                      - id
                      - identityProviderRef
                      type: object
                    certificate:
                      description: AccessCertificateRule is part of the accesscertificaterule
                        configuration.
                      type: object
                    cloudflareAccountMember:
                      description: AccessCloudflareAccountMemberRule is part of the
                        accesscloudflareaccountmemberrule configuration.
                      properties:
                        accountId:
                          type: string
                      type: object
                    commonName:
                      description: AccessCommonNameRule is part of the accesscommonnamerule
                        configuration.
                      properties:
                        commonName:
                          type: string
                      required:
                      - commonName
                      type: object
                    devicePosture:
                      description: AccessDevicePostureRuleReference is part of the
                        accessdeviceposturerulereference configuration.
                      properties:
                        ruleRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      required:
                      - ruleRef
                      type: object
                    email:
                      description: AccessEmailRule matches one email address.
                      properties:
                        email:
                          type: string
                      required:
                      - email
                      type: object
                    emailDomain:
                      description: AccessEmailDomainRule matches an email domain.
                      properties:
                        domain:
                          type: string
                      required:
                      - domain
                      type: object
                    emailList:
                      description: AccessListRule matches members of a Cloudflare
                        Access list.
                      properties:
                        listId:
                          type: string
                        listRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      type: object
                      x-kubernetes-validations:
                      - message: exactly one of listRef or listId is required
                        rule: has(self.listRef) != has(self.listId)
                    everyone:
                      description: AccessEveryoneRule matches everyone.
                      type: object
                    externalEvaluation:
                      description: AccessExternalEvaluationRule is part of the accessexternalevaluationrule
                        configuration.
                      properties:
                        evaluateUrl:
                          type: string
                        keysUrl:
                          type: string
                      required:
                      - evaluateUrl
                      - keysUrl
                      type: object
                    geo:
                      description: AccessGeoRule is part of the accessgeorule configuration.
                      properties:
                        countryCode:
                          type: string
                      required:
                      - countryCode
                      type: object
                    githubOrganization:
                      description: AccessGitHubOrganizationRule is part of the accessgithuborganizationrule
                        configuration.
                      properties:
                        identityProviderRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                        name:
                          type: string
                        team:
                          type: string
                      required:
                      - identityProviderRef
                      - name
                      type: object
                    group:
                      description: AccessGroupRule is part of the accessgrouprule
                        configuration.
                      properties:
                        groupRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      required:
                      - groupRef
                      type: object
                    gsuite:
                      description: AccessGSuiteRule is part of the accessgsuiterule
                        configuration.
                      properties:
                        email:
                          type: string
                        identityProviderRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      required:
                      - email
                      - identityProviderRef
                      type: object
                    ip:
                      description: AccessIPRule is part of the accessiprule configuration.
                      properties:
                        ip:
                          type: string
                      required:
                      - ip
                      type: object
                    ipList:
                      description: AccessListRule matches members of a Cloudflare
                        Access list.
                      properties:
                        listId:
                          type: string
                        listRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      type: object
                      x-kubernetes-validations:
                      - message: exactly one of listRef or listId is required
                        rule: has(self.listRef) != has(self.listId)
                    linkedAppToken:
                      description: AccessLinkedAppTokenRule is part of the accesslinkedapptokenrule
                        configuration.
                      properties:
                        appId:
                          type: string
                      required:
                      - appId
                      type: object
                    loginMethod:
                      description: AccessLoginMethodRule is part of the accessloginmethodrule
                        configuration.
                      properties:
                        identityProviderRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      required:
                      - identityProviderRef
                      type: object
                    oidc:
                      description: AccessOIDCRule is part of the accessoidcrule configuration.
                      properties:
                        claimName:
                          type: string
                        claimValue:
                          type: string
                        identityProviderRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      required:
                      - claimName
                      - claimValue
                      - identityProviderRef
                      type: object
                    okta:
                      description: AccessOktaRule is part of the accessoktarule configuration.
                      properties:
                        identityProviderRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                        name:
                          type: string
                      required:
                      - identityProviderRef
                      - name
                      type: object
                    saml:
                      description: AccessSAMLRule is part of the accesssamlrule configuration.
                      properties:
                        attributeName:
                          type: string
                        attributeValue:
                          type: string
                        identityProviderRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      required:
                      - attributeName
                      - attributeValue
                      - identityProviderRef
                      type: object
                    serviceToken:
                      description: AccessServiceTokenRule is part of the accessservicetokenrule
                        configuration.
                      properties:
                        tokenRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      required:
                      - tokenRef
                      type: object
                    userRiskScore:
                      description: AccessUserRiskScoreRule is part of the accessuserriskscorerule
                        configuration.
                      properties:
                        levels:
                          items:
                            description: AccessUserRiskLevel identifies a user risk
                              level.
                            enum:
                            - low
                            - medium
                            - high
                            - unscored
                            type: string
                          type: array
                      required:
                      - levels
                      type: object
                  type: object
                  x-kubernetes-validations:
                  - message: exactly one Access rule field is required
                    rule: (has(self.email)?1:0)+(has(self.emailDomain)?1:0)+(has(self.emailList)?1:0)+(has(self.everyone)?1:0)+(has(self.ip)?1:0)+(has(self.ipList)?1:0)+(has(self.certificate)?1:0)+(has(self.commonName)?1:0)+(has(self.group)?1:0)+(has(self.azureAD)?1:0)+(has(self.githubOrganization)?1:0)+(has(self.gsuite)?1:0)+(has(self.okta)?1:0)+(has(self.saml)?1:0)+(has(self.oidc)?1:0)+(has(self.serviceToken)?1:0)+(has(self.anyValidServiceToken)?1:0)+(has(self.externalEvaluation)?1:0)+(has(self.geo)?1:0)+(has(self.authMethod)?1:0)+(has(self.devicePosture)?1:0)+(has(self.loginMethod)?1:0)+(has(self.authContext)?1:0)+(has(self.linkedAppToken)?1:0)+(has(self.userRiskScore)?1:0)+(has(self.cloudflareAccountMember)?1:0)
                      == 1
                maxItems: 100
                type: array
                x-kubernetes-list-type: atomic
              externalRef:
                description: AccessPolicyExternalReference identifies an existing
                  Cloudflare Access policy.
                properties:
                  policyId:
                    minLength: 1
                    type: string
                required:
                - policyId
                type: object
              include:
                items:
                  description: AccessRule is a typed union of every reusable Access
                    rule supported by Cloudflare.
                  maxProperties: 1
                  minProperties: 1
                  properties:
                    anyValidServiceToken:
                      description: AccessAnyValidServiceTokenRule is part of the accessanyvalidservicetokenrule
                        configuration.
                      type: object
                    authContext:
                      description: AccessAuthContextRule is part of the accessauthcontextrule
                        configuration.
                      properties:
                        acId:
                          type: string
                        id:
                          type: string
                        identityProviderRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      required:
                      - acId
                      - id
                      - identityProviderRef
                      type: object
                    authMethod:
                      description: AccessAuthMethodRule is part of the accessauthmethodrule
                        configuration.
                      properties:
                        authMethod:
                          type: string
                      required:
                      - authMethod
                      type: object
                    azureAD:
                      description: AccessAzureADRule is part of the accessazureadrule
                        configuration.
                      properties:
                        id:
                          type: string
                        identityProviderRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      required:
                      - id
                      - identityProviderRef
                      type: object
                    certificate:
                      description: AccessCertificateRule is part of the accesscertificaterule
                        configuration.
                      type: object
                    cloudflareAccountMember:
                      description: AccessCloudflareAccountMemberRule is part of the
                        accesscloudflareaccountmemberrule configuration.
                      properties:
                        accountId:
                          type: string
                      type: object
                    commonName:
                      description: AccessCommonNameRule is part of the accesscommonnamerule
                        configuration.
                      properties:
                        commonName:
                          type: string
                      required:
                      - commonName
                      type: object
                    devicePosture:
                      description: AccessDevicePostureRuleReference is part of the
                        accessdeviceposturerulereference configuration.
                      properties:
                        ruleRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      required:
                      - ruleRef
                      type: object
                    email:
                      description: AccessEmailRule matches one email address.
                      properties:
                        email:
                          type: string
                      required:
                      - email
                      type: object
                    emailDomain:
                      description: AccessEmailDomainRule matches an email domain.
                      properties:
                        domain:
                          type: string
                      required:
                      - domain
                      type: object
                    emailList:
                      description: AccessListRule matches members of a Cloudflare
                        Access list.
                      properties:
                        listId:
                          type: string
                        listRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      type: object
                      x-kubernetes-validations:
                      - message: exactly one of listRef or listId is required
                        rule: has(self.listRef) != has(self.listId)
                    everyone:
                      description: AccessEveryoneRule matches everyone.
                      type: object
                    externalEvaluation:
                      description: AccessExternalEvaluationRule is part of the accessexternalevaluationrule
                        configuration.
                      properties:
                        evaluateUrl:
                          type: string
                        keysUrl:
                          type: string
                      required:
                      - evaluateUrl
                      - keysUrl
                      type: object
                    geo:
                      description: AccessGeoRule is part of the accessgeorule configuration.
                      properties:
                        countryCode:
                          type: string
                      required:
                      - countryCode
                      type: object
                    githubOrganization:
                      description: AccessGitHubOrganizationRule is part of the accessgithuborganizationrule
                        configuration.
                      properties:
                        identityProviderRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                        name:
                          type: string
                        team:
                          type: string
                      required:
                      - identityProviderRef
                      - name
                      type: object
                    group:
                      description: AccessGroupRule is part of the accessgrouprule
                        configuration.
                      properties:
                        groupRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      required:
                      - groupRef
                      type: object
                    gsuite:
                      description: AccessGSuiteRule is part of the accessgsuiterule
                        configuration.
                      properties:
                        email:
                          type: string
                        identityProviderRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      required:
                      - email
                      - identityProviderRef
                      type: object
                    ip:
                      description: AccessIPRule is part of the accessiprule configuration.
                      properties:
                        ip:
                          type: string
                      required:
                      - ip
                      type: object
                    ipList:
                      description: AccessListRule matches members of a Cloudflare
                        Access list.
                      properties:
                        listId:
                          type: string
                        listRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      type: object
                      x-kubernetes-validations:
                      - message: exactly one of listRef or listId is required
                        rule: has(self.listRef) != has(self.listId)
                    linkedAppToken:
                      description: AccessLinkedAppTokenRule is part of the accesslinkedapptokenrule
                        configuration.
                      properties:
                        appId:
                          type: string
                      required:
                      - appId
                      type: object
                    loginMethod:
                      description: AccessLoginMethodRule is part of the accessloginmethodrule
                        configuration.
                      properties:
                        identityProviderRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      required:
                      - identityProviderRef
                      type: object
                    oidc:
                      description: AccessOIDCRule is part of the accessoidcrule configuration.
                      properties:
                        claimName:
                          type: string
                        claimValue:
                          type: string
                        identityProviderRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      required:
                      - claimName
                      - claimValue
                      - identityProviderRef
                      type: object
                    okta:
                      description: AccessOktaRule is part of the accessoktarule configuration.
                      properties:
                        identityProviderRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                        name:
                          type: string
                      required:
                      - identityProviderRef
                      - name
                      type: object
                    saml:
                      description: AccessSAMLRule is part of the accesssamlrule configuration.
                      properties:
                        attributeName:
                          type: string
                        attributeValue:
                          type: string
                        identityProviderRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      required:
                      - attributeName
                      - attributeValue
                      - identityProviderRef
                      type: object
                    serviceToken:
                      description: AccessServiceTokenRule is part of the accessservicetokenrule
                        configuration.
                      properties:
                        tokenRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      required:
                      - tokenRef
                      type: object
                    userRiskScore:
                      description: AccessUserRiskScoreRule is part of the accessuserriskscorerule
                        configuration.
                      properties:
                        levels:
                          items:
                            description: AccessUserRiskLevel identifies a user risk
                              level.
                            enum:
                            - low
                            - medium
                            - high
                            - unscored
                            type: string
                          type: array
                      required:
                      - levels
                      type: object
                  type: object
                  x-kubernetes-validations:
                  - message: exactly one Access rule field is required
                    rule: (has(self.email)?1:0)+(has(self.emailDomain)?1:0)+(has(self.emailList)?1:0)+(has(self.everyone)?1:0)+(has(self.ip)?1:0)+(has(self.ipList)?1:0)+(has(self.certificate)?1:0)+(has(self.commonName)?1:0)+(has(self.group)?1:0)+(has(self.azureAD)?1:0)+(has(self.githubOrganization)?1:0)+(has(self.gsuite)?1:0)+(has(self.okta)?1:0)+(has(self.saml)?1:0)+(has(self.oidc)?1:0)+(has(self.serviceToken)?1:0)+(has(self.anyValidServiceToken)?1:0)+(has(self.externalEvaluation)?1:0)+(has(self.geo)?1:0)+(has(self.authMethod)?1:0)+(has(self.devicePosture)?1:0)+(has(self.loginMethod)?1:0)+(has(self.authContext)?1:0)+(has(self.linkedAppToken)?1:0)+(has(self.userRiskScore)?1:0)+(has(self.cloudflareAccountMember)?1:0)
                      == 1
                maxItems: 100
                minItems: 1
                type: array
                x-kubernetes-list-type: atomic
              isolationRequired:
                type: boolean
              managementPolicy:
                default: Managed
                description: ManagementPolicy controls whether Flareway mutates a
                  remote resource.
                enum:
                - Managed
                - ObserveOnly
                type: string
              name:
                minLength: 1
                type: string
              purposeJustification:
                description: AccessPolicyPurposeJustification configures purpose justification
                  prompts.
                properties:
                  prompt:
                    type: string
                  required:
                    type: boolean
                type: object
              require:
                items:
                  description: AccessRule is a typed union of every reusable Access
                    rule supported by Cloudflare.
                  maxProperties: 1
                  minProperties: 1
                  properties:
                    anyValidServiceToken:
                      description: AccessAnyValidServiceTokenRule is part of the accessanyvalidservicetokenrule
                        configuration.
                      type: object
                    authContext:
                      description: AccessAuthContextRule is part of the accessauthcontextrule
                        configuration.
                      properties:
                        acId:
                          type: string
                        id:
                          type: string
                        identityProviderRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      required:
                      - acId
                      - id
                      - identityProviderRef
                      type: object
                    authMethod:
                      description: AccessAuthMethodRule is part of the accessauthmethodrule
                        configuration.
                      properties:
                        authMethod:
                          type: string
                      required:
                      - authMethod
                      type: object
                    azureAD:
                      description: AccessAzureADRule is part of the accessazureadrule
                        configuration.
                      properties:
                        id:
                          type: string
                        identityProviderRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      required:
                      - id
                      - identityProviderRef
                      type: object
                    certificate:
                      description: AccessCertificateRule is part of the accesscertificaterule
                        configuration.
                      type: object
                    cloudflareAccountMember:
                      description: AccessCloudflareAccountMemberRule is part of the
                        accesscloudflareaccountmemberrule configuration.
                      properties:
                        accountId:
                          type: string
                      type: object
                    commonName:
                      description: AccessCommonNameRule is part of the accesscommonnamerule
                        configuration.
                      properties:
                        commonName:
                          type: string
                      required:
                      - commonName
                      type: object
                    devicePosture:
                      description: AccessDevicePostureRuleReference is part of the
                        accessdeviceposturerulereference configuration.
                      properties:
                        ruleRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      required:
                      - ruleRef
                      type: object
                    email:
                      description: AccessEmailRule matches one email address.
                      properties:
                        email:
                          type: string
                      required:
                      - email
                      type: object
                    emailDomain:
                      description: AccessEmailDomainRule matches an email domain.
                      properties:
                        domain:
                          type: string
                      required:
                      - domain
                      type: object
                    emailList:
                      description: AccessListRule matches members of a Cloudflare
                        Access list.
                      properties:
                        listId:
                          type: string
                        listRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      type: object
                      x-kubernetes-validations:
                      - message: exactly one of listRef or listId is required
                        rule: has(self.listRef) != has(self.listId)
                    everyone:
                      description: AccessEveryoneRule matches everyone.
                      type: object
                    externalEvaluation:
                      description: AccessExternalEvaluationRule is part of the accessexternalevaluationrule
                        configuration.
                      properties:
                        evaluateUrl:
                          type: string
                        keysUrl:
                          type: string
                      required:
                      - evaluateUrl
                      - keysUrl
                      type: object
                    geo:
                      description: AccessGeoRule is part of the accessgeorule configuration.
                      properties:
                        countryCode:
                          type: string
                      required:
                      - countryCode
                      type: object
                    githubOrganization:
                      description: AccessGitHubOrganizationRule is part of the accessgithuborganizationrule
                        configuration.
                      properties:
                        identityProviderRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                        name:
                          type: string
                        team:
                          type: string
                      required:
                      - identityProviderRef
                      - name
                      type: object
                    group:
                      description: AccessGroupRule is part of the accessgrouprule
                        configuration.
                      properties:
                        groupRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      required:
                      - groupRef
                      type: object
                    gsuite:
                      description: AccessGSuiteRule is part of the accessgsuiterule
                        configuration.
                      properties:
                        email:
                          type: string
                        identityProviderRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      required:
                      - email
                      - identityProviderRef
                      type: object
                    ip:
                      description: AccessIPRule is part of the accessiprule configuration.
                      properties:
                        ip:
                          type: string
                      required:
                      - ip
                      type: object
                    ipList:
                      description: AccessListRule matches members of a Cloudflare
                        Access list.
                      properties:
                        listId:
                          type: string
                        listRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      type: object
                      x-kubernetes-validations:
                      - message: exactly one of listRef or listId is required
                        rule: has(self.listRef) != has(self.listId)
                    linkedAppToken:
                      description: AccessLinkedAppTokenRule is part of the accesslinkedapptokenrule
                        configuration.
                      properties:
                        appId:
                          type: string
                      required:
                      - appId
                      type: object
                    loginMethod:
                      description: AccessLoginMethodRule is part of the accessloginmethodrule
                        configuration.
                      properties:
                        identityProviderRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      required:
                      - identityProviderRef
                      type: object
                    oidc:
                      description: AccessOIDCRule is part of the accessoidcrule configuration.
                      properties:
                        claimName:
                          type: string
                        claimValue:
                          type: string
                        identityProviderRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      required:
                      - claimName
                      - claimValue
                      - identityProviderRef
                      type: object
                    okta:
                      description: AccessOktaRule is part of the accessoktarule configuration.
                      properties:
                        identityProviderRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                        name:
                          type: string
                      required:
                      - identityProviderRef
                      - name
                      type: object
                    saml:
                      description: AccessSAMLRule is part of the accesssamlrule configuration.
                      properties:
                        attributeName:
                          type: string
                        attributeValue:
                          type: string
                        identityProviderRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      required:
                      - attributeName
                      - attributeValue
                      - identityProviderRef
                      type: object
                    serviceToken:
                      description: AccessServiceTokenRule is part of the accessservicetokenrule
                        configuration.
                      properties:
                        tokenRef:
                          description: AccessObjectReference selects a local managed
                            object or a remote object ID.
                          properties:
                            externalId:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of name or externalId is required
                            rule: has(self.name) != has(self.externalId)
                      required:
                      - tokenRef
                      type: object
                    userRiskScore:
                      description: AccessUserRiskScoreRule is part of the accessuserriskscorerule
                        configuration.
                      properties:
                        levels:
                          items:
                            description: AccessUserRiskLevel identifies a user risk
                              level.
                            enum:
                            - low
                            - medium
                            - high
                            - unscored
                            type: string
                          type: array
                      required:
                      - levels
                      type: object
                  type: object
                  x-kubernetes-validations:
                  - message: exactly one Access rule field is required
                    rule: (has(self.email)?1:0)+(has(self.emailDomain)?1:0)+(has(self.emailList)?1:0)+(has(self.everyone)?1:0)+(has(self.ip)?1:0)+(has(self.ipList)?1:0)+(has(self.certificate)?1:0)+(has(self.commonName)?1:0)+(has(self.group)?1:0)+(has(self.azureAD)?1:0)+(has(self.githubOrganization)?1:0)+(has(self.gsuite)?1:0)+(has(self.okta)?1:0)+(has(self.saml)?1:0)+(has(self.oidc)?1:0)+(has(self.serviceToken)?1:0)+(has(self.anyValidServiceToken)?1:0)+(has(self.externalEvaluation)?1:0)+(has(self.geo)?1:0)+(has(self.authMethod)?1:0)+(has(self.devicePosture)?1:0)+(has(self.loginMethod)?1:0)+(has(self.authContext)?1:0)+(has(self.linkedAppToken)?1:0)+(has(self.userRiskScore)?1:0)+(has(self.cloudflareAccountMember)?1:0)
                      == 1
                maxItems: 100
                type: array
                x-kubernetes-list-type: atomic
              sessionDuration:
                type: string
            required:
            - accountRef
            - decision
            - include
            - name
            type: object
            x-kubernetes-validations:
            - message: ObserveOnly requires externalRef
              rule: self.managementPolicy != 'ObserveOnly' || has(self.externalRef)
            - message: AdoptById requires externalRef
              rule: self.adoption.mode != 'AdoptById' || has(self.externalRef)
          status:
            description: AccessPolicyStatus records the remote policy identity and
              references.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                format: int64
                type: integer
              ownershipVerified:
                type: boolean
              policyId:
                type: string
              referencedBy:
                items:
                  description: AccessPolicyReferenceStatus identifies a resource referencing
                    this policy.
                  properties:
                    name:
                      type: string
                    namespace:
                      type: string
                  required:
                  - name
                  - namespace
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - namespace
                - name
                x-kubernetes-list-type: map
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
