---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  labels:
    gateway.networking.k8s.io/policy: Direct
  name: accessapplications.flareway.bhyoo.com
spec:
  group: flareway.bhyoo.com
  names:
    categories:
    - flareway
    kind: AccessApplication
    listKind: AccessApplicationList
    plural: accessapplications
    shortNames:
    - cfaa
    singular: accessapplication
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Accepted")].status
      name: Accepted
      type: string
    - jsonPath: .status.applicationId
      name: Application
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AccessApplication attaches Cloudflare Access to Gateway API targets.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AccessApplicationSpec defines one Gateway-attached Cloudflare
              Access application.
            properties:
              accountRef:
                description: |-
                  LocalObjectReference contains enough information to let you locate the
                  referenced object inside the same namespace.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              adoption:
                default: {}
                description: AdoptionSpec configures explicit adoption of an existing
                  remote object.
                properties:
                  expect:
                    description: AdoptionExpect declares remote attributes that must
                      match before adoption.
                    properties:
                      domain:
                        maxLength: 253
                        type: string
                      name:
                        maxLength: 100
                        type: string
                    type: object
                  mode:
                    default: None
                    description: AdoptionMode controls explicit adoption of a remote
                      resource.
                    enum:
                    - None
                    - AdoptById
                    type: string
                type: object
              application:
                description: AccessApplicationSettings contains fields shared by Access
                  application variants.
                properties:
                  allowAuthenticateViaWarp:
                    type: boolean
                  allowIframe:
                    type: boolean
                  allowedIdpRefs:
                    items:
                      description: AccessIdentityProviderReference selects a managed
                        IdP or an external IdP ID.
                      properties:
                        externalId:
                          minLength: 1
                          type: string
                        name:
                          minLength: 1
                          type: string
                      type: object
                      x-kubernetes-validations:
                      - message: exactly one of name or externalId is required
                        rule: has(self.name) != has(self.externalId)
                    type: array
                    x-kubernetes-list-type: atomic
                  appLauncherVisible:
                    type: boolean
                  autoRedirectToIdentity:
                    type: boolean
                  corsHeaders:
                    description: AccessCORSHeaders configures Access edge CORS handling.
                    properties:
                      allowAllHeaders:
                        type: boolean
                      allowAllMethods:
                        type: boolean
                      allowAllOrigins:
                        type: boolean
                      allowCredentials:
                        type: boolean
                      allowedHeaders:
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      allowedMethods:
                        items:
                          description: AccessCORSMethod is an HTTP method accepted
                            by Cloudflare Access CORS settings.
                          enum:
                          - GET
                          - POST
                          - HEAD
                          - PUT
                          - DELETE
                          - CONNECT
                          - OPTIONS
                          - TRACE
                          - PATCH
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      allowedOrigins:
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      maxAge:
                        format: int64
                        type: integer
                    type: object
                  customDenyMessage:
                    type: string
                  customDenyUrl:
                    type: string
                  customNonIdentityDenyUrl:
                    type: string
                  customPageRefs:
                    items:
                      description: AccessCustomPageReference selects a managed AccessCustomPage
                        or an external page ID.
                      properties:
                        externalId:
                          minLength: 1
                          type: string
                        objectRef:
                          description: NamespacedLocalObjectReference identifies a
                            namespaced Flareway resource.
                          properties:
                            name:
                              minLength: 1
                              type: string
                            namespace:
                              description: Namespace defaults to the referencing object's
                                namespace.
                              type: string
                          required:
                          - name
                          type: object
                      type: object
                      x-kubernetes-validations:
                      - message: exactly one of objectRef or externalId is required
                        rule: has(self.objectRef) != has(self.externalId)
                    maxItems: 25
                    type: array
                    x-kubernetes-list-type: atomic
                  eagerRedirectCookieSetting:
                    type: boolean
                  enableBindingCookie:
                    type: boolean
                  httpOnlyCookieAttribute:
                    type: boolean
                  logoUrl:
                    type: string
                  mfaConfig:
                    description: AccessApplicationMFAConfig configures application-level
                      MFA.
                    properties:
                      allowedAuthenticators:
                        items:
                          description: AccessApplicationMFAAuthenticator is an application-level
                            MFA method.
                          enum:
                          - TOTP
                          - Biometrics
                          - SecurityKey
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      mfaDisabled:
                        type: boolean
                      sessionDuration:
                        pattern: ^([0-9]+(ns|us|ms|s|m|h))+$
                        type: string
                    type: object
                  name:
                    type: string
                  oauthConfiguration:
                    description: AccessApplicationOAuthConfiguration makes Access
                      an OAuth authorization server.
                    properties:
                      dynamicClientRegistration:
                        description: AccessOAuthDynamicClientRegistration configures
                          OAuth dynamic clients.
                        properties:
                          allowAnyOnLocalhost:
                            type: boolean
                          allowAnyOnLoopback:
                            type: boolean
                          allowedUris:
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: set
                          enabled:
                            type: boolean
                        type: object
                      enabled:
                        type: boolean
                      grant:
                        description: AccessOAuthGrant configures OAuth token and session
                          lifetimes.
                        properties:
                          accessTokenLifetime:
                            pattern: ^([0-9]+(ns|us|ms|s|m|h))+$
                            type: string
                          sessionDuration:
                            pattern: ^([0-9]+(ns|us|ms|s|m|h))+$
                            type: string
                        type: object
                    type: object
                  optionsPreflightBypass:
                    type: boolean
                  pathCookieAttribute:
                    type: boolean
                  readServiceTokensFromHeader:
                    type: string
                  sameSiteCookieAttribute:
                    description: AccessSameSiteCookieAttribute configures the SameSite
                      cookie attribute.
                    enum:
                    - Lax
                    - Strict
                    - None
                    type: string
                  scimConfig:
                    description: AccessApplicationSCIMConfig configures outbound SCIM
                      provisioning.
                    properties:
                      authentication:
                        description: AccessSCIMAuthentication selects one authentication
                          method or an ordered set.
                        properties:
                          accessServiceToken:
                            description: AccessSCIMAccessServiceTokenAuthentication
                              selects a managed ServiceToken.
                            properties:
                              serviceTokenRef:
                                description: NamespacedLocalObjectReference identifies
                                  a namespaced Flareway resource.
                                properties:
                                  name:
                                    minLength: 1
                                    type: string
                                  namespace:
                                    description: Namespace defaults to the referencing
                                      object's namespace.
                                    type: string
                                required:
                                - name
                                type: object
                            required:
                            - serviceTokenRef
                            type: object
                          httpBasic:
                            description: AccessSCIMHTTPBasicAuthentication configures
                              SCIM HTTP Basic authentication.
                            properties:
                              passwordSecretRef:
                                description: AccessApplicationSecretKeyReference identifies
                                  one key in a Secret in the same namespace.
                                properties:
                                  key:
                                    minLength: 1
                                    type: string
                                  name:
                                    minLength: 1
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                              user:
                                minLength: 1
                                type: string
                            required:
                            - passwordSecretRef
                            - user
                            type: object
                          multiple:
                            items:
                              description: AccessSCIMAuthenticationMethod is one member
                                of a multi-authentication list.
                              properties:
                                accessServiceToken:
                                  description: AccessSCIMAccessServiceTokenAuthentication
                                    selects a managed ServiceToken.
                                  properties:
                                    serviceTokenRef:
                                      description: NamespacedLocalObjectReference
                                        identifies a namespaced Flareway resource.
                                      properties:
                                        name:
                                          minLength: 1
                                          type: string
                                        namespace:
                                          description: Namespace defaults to the referencing
                                            object's namespace.
                                          type: string
                                      required:
                                      - name
                                      type: object
                                  required:
                                  - serviceTokenRef
                                  type: object
                                httpBasic:
                                  description: AccessSCIMHTTPBasicAuthentication configures
                                    SCIM HTTP Basic authentication.
                                  properties:
                                    passwordSecretRef:
                                      description: AccessApplicationSecretKeyReference
                                        identifies one key in a Secret in the same
                                        namespace.
                                      properties:
                                        key:
                                          minLength: 1
                                          type: string
                                        name:
                                          minLength: 1
                                          type: string
                                      required:
                                      - key
                                      - name
                                      type: object
                                    user:
                                      minLength: 1
                                      type: string
                                  required:
                                  - passwordSecretRef
                                  - user
                                  type: object
                                oauth2:
                                  description: AccessSCIMOAuth2Authentication configures
                                    OAuth 2 client credentials for SCIM.
                                  properties:
                                    authorizationUrl:
                                      minLength: 1
                                      type: string
                                    clientId:
                                      minLength: 1
                                      type: string
                                    clientSecretRef:
                                      description: AccessApplicationSecretKeyReference
                                        identifies one key in a Secret in the same
                                        namespace.
                                      properties:
                                        key:
                                          minLength: 1
                                          type: string
                                        name:
                                          minLength: 1
                                          type: string
                                      required:
                                      - key
                                      - name
                                      type: object
                                    scopes:
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    tokenUrl:
                                      minLength: 1
                                      type: string
                                  required:
                                  - authorizationUrl
                                  - clientId
                                  - clientSecretRef
                                  - tokenUrl
                                  type: object
                                oauthBearerToken:
                                  description: AccessSCIMOAuthBearerTokenAuthentication
                                    configures a static bearer token.
                                  properties:
                                    tokenSecretRef:
                                      description: AccessApplicationSecretKeyReference
                                        identifies one key in a Secret in the same
                                        namespace.
                                      properties:
                                        key:
                                          minLength: 1
                                          type: string
                                        name:
                                          minLength: 1
                                          type: string
                                      required:
                                      - key
                                      - name
                                      type: object
                                  required:
                                  - tokenSecretRef
                                  type: object
                              type: object
                              x-kubernetes-validations:
                              - message: exactly one SCIM authentication method is
                                  required
                                rule: (has(self.httpBasic)?1:0)+(has(self.oauthBearerToken)?1:0)+(has(self.oauth2)?1:0)+(has(self.accessServiceToken)?1:0)
                                  == 1
                            maxItems: 4
                            minItems: 1
                            type: array
                            x-kubernetes-list-type: atomic
                          oauth2:
                            description: AccessSCIMOAuth2Authentication configures
                              OAuth 2 client credentials for SCIM.
                            properties:
                              authorizationUrl:
                                minLength: 1
                                type: string
                              clientId:
                                minLength: 1
                                type: string
                              clientSecretRef:
                                description: AccessApplicationSecretKeyReference identifies
                                  one key in a Secret in the same namespace.
                                properties:
                                  key:
                                    minLength: 1
                                    type: string
                                  name:
                                    minLength: 1
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                              scopes:
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              tokenUrl:
                                minLength: 1
                                type: string
                            required:
                            - authorizationUrl
                            - clientId
                            - clientSecretRef
                            - tokenUrl
                            type: object
                          oauthBearerToken:
                            description: AccessSCIMOAuthBearerTokenAuthentication
                              configures a static bearer token.
                            properties:
                              tokenSecretRef:
                                description: AccessApplicationSecretKeyReference identifies
                                  one key in a Secret in the same namespace.
                                properties:
                                  key:
                                    minLength: 1
                                    type: string
                                  name:
                                    minLength: 1
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                            required:
                            - tokenSecretRef
                            type: object
                        type: object
                        x-kubernetes-validations:
                        - message: exactly one SCIM authentication variant is required
                          rule: (has(self.httpBasic)?1:0)+(has(self.oauthBearerToken)?1:0)+(has(self.oauth2)?1:0)+(has(self.accessServiceToken)?1:0)+(has(self.multiple)?1:0)
                            == 1
                      deactivateOnDelete:
                        type: boolean
                      enabled:
                        type: boolean
                      idpRef:
                        description: AccessIdentityProviderReference selects a managed
                          IdP or an external IdP ID.
                        properties:
                          externalId:
                            minLength: 1
                            type: string
                          name:
                            minLength: 1
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: exactly one of name or externalId is required
                          rule: has(self.name) != has(self.externalId)
                      mappings:
                        items:
                          description: AccessSCIMMapping transforms or filters one
                            SCIM resource type.
                          properties:
                            enabled:
                              type: boolean
                            filter:
                              type: string
                            operations:
                              description: AccessSCIMMappingOperations selects SCIM
                                operations for one mapping.
                              properties:
                                create:
                                  type: boolean
                                delete:
                                  type: boolean
                                update:
                                  type: boolean
                              type: object
                            schema:
                              minLength: 1
                              type: string
                            strictness:
                              description: AccessSCIMMappingStrictness controls handling
                                of unknown SCIM values.
                              enum:
                              - Strict
                              - Passthrough
                              type: string
                            transformJsonata:
                              type: string
                          required:
                          - schema
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      remoteUri:
                        minLength: 1
                        type: string
                    required:
                    - idpRef
                    - remoteUri
                    type: object
                  serviceAuth401Redirect:
                    type: boolean
                  sessionDuration:
                    pattern: ^([0-9]+(ns|us|ms|s|m|h))+$
                    type: string
                  skipInterstitial:
                    type: boolean
                  tags:
                    items:
                      maxLength: 35
                      pattern: ^[A-Za-z0-9_-]+$
                      type: string
                      x-kubernetes-validations:
                      - message: tag names starting with flareway- are reserved by
                          the operator
                        rule: '!self.startsWith(''flareway-'')'
                    maxItems: 23
                    type: array
                    x-kubernetes-list-type: set
                  useClientlessIsolationAppLauncherUrl:
                    type: boolean
                type: object
                x-kubernetes-validations:
                - message: optionsPreflightBypass cannot be true when corsHeaders
                    is set
                  rule: '!has(self.optionsPreflightBypass) || !self.optionsPreflightBypass
                    || !has(self.corsHeaders)'
                - message: autoRedirectToIdentity requires exactly one allowedIdpRef
                  rule: '!has(self.autoRedirectToIdentity) || !self.autoRedirectToIdentity
                    || (has(self.allowedIdpRefs) && size(self.allowedIdpRefs) == 1)'
              bypass:
                default: {}
                description: AccessBypassSpec declares adoptable identities for carve-outs
                  compiled from routes.
                properties:
                  children:
                    items:
                      description: AccessBypassChildSpec declares remote identity
                        and adoption for one compiled carve-out.
                      properties:
                        adoption:
                          default: {}
                          description: AdoptionSpec configures explicit adoption of
                            an existing remote object.
                          properties:
                            expect:
                              description: AdoptionExpect declares remote attributes
                                that must match before adoption.
                              properties:
                                domain:
                                  maxLength: 253
                                  type: string
                                name:
                                  maxLength: 100
                                  type: string
                              type: object
                            mode:
                              default: None
                              description: AdoptionMode controls explicit adoption
                                of a remote resource.
                              enum:
                              - None
                              - AdoptById
                              type: string
                          type: object
                        deletionPolicy:
                          default: Orphan
                          description: DeletionPolicy controls whether a managed remote
                            resource is deleted or orphaned.
                          enum:
                          - Delete
                          - Orphan
                          type: string
                        externalRef:
                          description: AccessApplicationExternalReference identifies
                            an existing Access application.
                          properties:
                            applicationId:
                              minLength: 1
                              type: string
                          required:
                          - applicationId
                          type: object
                        hostname:
                          minLength: 1
                          type: string
                        name:
                          type: string
                        path:
                          minLength: 1
                          pattern: ^/
                          type: string
                        policyRef:
                          description: |-
                            AccessApplicationPolicyReference selects a managed policy or an external policy ID.
                            Slice order defines remote policy precedence.
                          properties:
                            externalRef:
                              description: AccessApplicationPolicyExternalReference
                                identifies a reusable Access policy.
                              properties:
                                policyId:
                                  minLength: 1
                                  type: string
                              required:
                              - policyId
                              type: object
                            policyRef:
                              description: NamespacedLocalObjectReference identifies
                                a namespaced Flareway resource.
                              properties:
                                name:
                                  minLength: 1
                                  type: string
                                namespace:
                                  description: Namespace defaults to the referencing
                                    object's namespace.
                                  type: string
                              required:
                              - name
                              type: object
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of policyRef or externalRef is required
                            rule: has(self.policyRef) != has(self.externalRef)
                      required:
                      - hostname
                      - path
                      type: object
                      x-kubernetes-validations:
                      - message: AdoptById requires externalRef
                        rule: self.adoption.mode != 'AdoptById' || has(self.externalRef)
                    maxItems: 100
                    type: array
                    x-kubernetes-list-map-keys:
                    - hostname
                    - path
                    x-kubernetes-list-type: map
                type: object
              deletionPolicy:
                default: Delete
                description: DeletionPolicy controls whether a managed remote resource
                  is deleted or orphaned.
                enum:
                - Delete
                - Orphan
                type: string
              destinations:
                description: |-
                  Destinations adds Cloudflare-side private, MCP, and Worker destinations.
                  Public destinations are derived from TargetRefs. The deprecated
                  self_hosted_domains wire field is intentionally not exposed.
                items:
                  description: AccessApplicationDestinationSpec is a discriminated
                    destination union.
                  properties:
                    allPreviewWorkers:
                      description: AccessAllPreviewWorkersDestinationSpec selects
                        all Worker previews.
                      type: object
                    allWorkers:
                      description: AccessAllWorkersDestinationSpec selects all production
                        Workers.
                      type: object
                    previewWorker:
                      description: AccessWorkerDestinationSpec selects a Cloudflare
                        Worker.
                      properties:
                        workerId:
                          minLength: 1
                          type: string
                      required:
                      - workerId
                      type: object
                    private:
                      description: AccessPrivateDestinationSpec selects one managed
                        private route.
                      properties:
                        cidr:
                          type: string
                        hostnameRouteRef:
                          description: |-
                            LocalObjectReference contains enough information to let you locate the
                            referenced object inside the same namespace.
                          properties:
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                        l4Protocol:
                          description: AccessL4Protocol selects a private destination
                            transport protocol. Omission matches both.
                          enum:
                          - TCP
                          - UDP
                          type: string
                        networkRouteRef:
                          description: |-
                            LocalObjectReference contains enough information to let you locate the
                            referenced object inside the same namespace.
                          properties:
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                        portRange:
                          pattern: ^[0-9]+(-[0-9]+)?$
                          type: string
                      type: object
                      x-kubernetes-validations:
                      - message: exactly one of networkRouteRef or hostnameRouteRef
                          is required
                        rule: has(self.networkRouteRef) != has(self.hostnameRouteRef)
                    public:
                      description: AccessPublicDestinationSpec directly identifies
                        a public hostname or path.
                      properties:
                        uri:
                          minLength: 1
                          type: string
                      required:
                      - uri
                      type: object
                    type:
                      description: AccessApplicationDestinationType selects a Cloudflare
                        Access destination variant.
                      enum:
                      - Public
                      - Private
                      - ViaMCPServerPortal
                      - Worker
                      - PreviewWorker
                      - AllWorkers
                      - AllPreviewWorkers
                      type: string
                    viaMcpServerPortal:
                      description: AccessMCPServerPortalDestinationSpec selects an
                        AI Controls MCP server.
                      properties:
                        mcpServerId:
                          minLength: 1
                          type: string
                      required:
                      - mcpServerId
                      type: object
                    worker:
                      description: AccessWorkerDestinationSpec selects a Cloudflare
                        Worker.
                      properties:
                        workerId:
                          minLength: 1
                          type: string
                      required:
                      - workerId
                      type: object
                  required:
                  - type
                  type: object
                  x-kubernetes-validations:
                  - message: exactly one destination variant is required
                    rule: (has(self.public)?1:0)+(has(self.private)?1:0)+(has(self.viaMcpServerPortal)?1:0)+(has(self.worker)?1:0)+(has(self.previewWorker)?1:0)+(has(self.allWorkers)?1:0)+(has(self.allPreviewWorkers)?1:0)
                      == 1
                  - message: public is required if and only if type is Public
                    rule: (self.type == 'Public') == has(self.public)
                  - message: private is required if and only if type is Private
                    rule: (self.type == 'Private') == has(self.private)
                  - message: viaMcpServerPortal is required if and only if type is
                      ViaMCPServerPortal
                    rule: (self.type == 'ViaMCPServerPortal') == has(self.viaMcpServerPortal)
                  - message: worker is required if and only if type is Worker
                    rule: (self.type == 'Worker') == has(self.worker)
                  - message: previewWorker is required if and only if type is PreviewWorker
                    rule: (self.type == 'PreviewWorker') == has(self.previewWorker)
                  - message: allWorkers is required if and only if type is AllWorkers
                    rule: (self.type == 'AllWorkers') == has(self.allWorkers)
                  - message: allPreviewWorkers is required if and only if type is
                      AllPreviewWorkers
                    rule: (self.type == 'AllPreviewWorkers') == has(self.allPreviewWorkers)
                maxItems: 100
                type: array
                x-kubernetes-list-type: atomic
              externalRef:
                description: AccessApplicationExternalReference identifies an existing
                  Access application.
                properties:
                  applicationId:
                    minLength: 1
                    type: string
                required:
                - applicationId
                type: object
              managementPolicy:
                default: Managed
                description: ManagementPolicy controls whether Flareway mutates a
                  remote resource.
                enum:
                - Managed
                - ObserveOnly
                type: string
              mcp:
                description: AccessMCPApplicationSpec selects the MCP server request
                  shape.
                type: object
              originJWT:
                default: {}
                description: AccessOriginJWTSpec configures origin JWT enforcement.
                properties:
                  assumeGatewayTLSDecryption:
                    type: boolean
                  audienceScope:
                    default: Application
                    description: AccessOriginJWTAudienceScope selects which application
                      audiences an origin accepts.
                    enum:
                    - Application
                    - Hostname
                    type: string
                  mode:
                    default: Required
                    description: AccessOriginJWTMode controls origin JWT verification.
                    enum:
                    - Required
                    - Disabled
                    type: string
                type: object
                x-kubernetes-validations:
                - message: Hostname audienceScope requires mode Required
                  rule: '!has(self.audienceScope) || self.audienceScope != ''Hostname''
                    || !has(self.mode) || self.mode == ''Required'''
              pathScope:
                description: AccessTargetPath narrows a Gateway attachment to a protected
                  path.
                properties:
                  type:
                    default: PathPrefix
                    description: |-
                      PathMatchType specifies the semantics of how HTTP paths should be compared.
                      Valid PathMatchType values, along with their support levels, are:

                      * "Exact" - Core
                      * "PathPrefix" - Core
                      * "RegularExpression" - Implementation Specific

                      PathPrefix and Exact paths must be syntactically valid:

                      - Must begin with the `/` character
                      - Must not contain consecutive `/` characters (e.g. `/foo///`, `//`).

                      Note that values may be added to this enum, implementations
                      must ensure that unknown values will not cause a crash.

                      Unknown values here must result in the implementation setting the
                      Accepted Condition for the Route to `status: False`, with a
                      Reason of `UnsupportedValue`.
                    enum:
                    - Exact
                    - PathPrefix
                    - RegularExpression
                    type: string
                  value:
                    minLength: 1
                    pattern: ^/
                    type: string
                required:
                - value
                type: object
              policies:
                items:
                  description: |-
                    AccessApplicationPolicyReference selects a managed policy or an external policy ID.
                    Slice order defines remote policy precedence.
                  properties:
                    externalRef:
                      description: AccessApplicationPolicyExternalReference identifies
                        a reusable Access policy.
                      properties:
                        policyId:
                          minLength: 1
                          type: string
                      required:
                      - policyId
                      type: object
                    policyRef:
                      description: NamespacedLocalObjectReference identifies a namespaced
                        Flareway resource.
                      properties:
                        name:
                          minLength: 1
                          type: string
                        namespace:
                          description: Namespace defaults to the referencing object's
                            namespace.
                          type: string
                      required:
                      - name
                      type: object
                  type: object
                  x-kubernetes-validations:
                  - message: exactly one of policyRef or externalRef is required
                    rule: has(self.policyRef) != has(self.externalRef)
                maxItems: 100
                minItems: 1
                type: array
                x-kubernetes-list-type: atomic
              proxyEndpoint:
                description: AccessProxyEndpointApplicationSpec selects the Gateway
                  proxy endpoint request shape.
                type: object
              rdp:
                description: AccessRDPApplicationSpec configures browser RDP target
                  criteria.
                properties:
                  targetCriteria:
                    items:
                      description: AccessTargetCriterion selects infrastructure targets
                        by port, protocol and attributes.
                      properties:
                        port:
                          format: int32
                          maximum: 65535
                          minimum: 1
                          type: integer
                        protocol:
                          description: AccessTargetProtocol is a target criterion
                            protocol.
                          enum:
                          - SSH
                          - TCP
                          - RDP
                          type: string
                        targetAttributes:
                          additionalProperties:
                            items:
                              type: string
                            type: array
                          minProperties: 1
                          type: object
                      required:
                      - port
                      - protocol
                      - targetAttributes
                      type: object
                    minItems: 1
                    type: array
                    x-kubernetes-list-type: atomic
                required:
                - targetCriteria
                type: object
                x-kubernetes-validations:
                - message: RDP target criteria require RDP protocol
                  rule: self.targetCriteria.all(c, c.protocol == 'RDP')
              selfHosted:
                description: AccessSelfHostedApplicationSpec selects the self-hosted
                  request shape.
                type: object
              ssh:
                description: AccessSSHApplicationSpec selects the browser SSH request
                  shape.
                type: object
              targetRefs:
                items:
                  description: |-
                    LocalPolicyTargetReferenceWithSectionName identifies an API object to apply a
                    direct policy to. This should be used as part of Policy resources that can
                    target single resources. For more information on how this policy attachment
                    mode works, and a sample Policy resource, refer to the policy attachment
                    documentation for Gateway API.

                    Note: This should only be used for direct policy attachment when references
                    to SectionName are actually needed. In all other cases,
                    LocalPolicyTargetReference should be used.
                  properties:
                    group:
                      description: Group is the group of the target resource.
                      maxLength: 253
                      pattern: ^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                      type: string
                    kind:
                      description: Kind is kind of the target resource.
                      maxLength: 63
                      minLength: 1
                      pattern: ^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$
                      type: string
                    name:
                      description: Name is the name of the target resource.
                      maxLength: 253
                      minLength: 1
                      type: string
                    sectionName:
                      description: |-
                        SectionName is the name of a section within the target resource. When
                        unspecified, this targetRef targets the entire resource. In the following
                        resources, SectionName is interpreted as the following:

                        * Gateway: Listener name
                        * HTTPRoute: HTTPRouteRule name
                        * Service: Port name

                        If a SectionName is specified, but does not exist on the targeted object,
                        the Policy must fail to attach, and the policy implementation should record
                        a `ResolvedRefs` or similar Condition in the Policy's status.
                      maxLength: 253
                      minLength: 1
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                      type: string
                  required:
                  - group
                  - kind
                  - name
                  type: object
                maxItems: 32
                type: array
                x-kubernetes-list-type: atomic
              type:
                description: AccessApplicationType selects a Gateway-attached Access
                  application variant.
                enum:
                - SelfHosted
                - SSH
                - VNC
                - RDP
                - MCP
                - ProxyEndpoint
                type: string
                x-kubernetes-validations:
                - message: type is immutable
                  rule: self == oldSelf
              vnc:
                description: AccessVNCApplicationSpec selects the browser VNC request
                  shape.
                type: object
              zone:
                description: Zone is an optional DNS zone name resolved through CloudflareAccount
                  status.
                minLength: 1
                type: string
            required:
            - accountRef
            - application
            - policies
            - type
            type: object
            x-kubernetes-validations:
            - message: accountRef.name is required
              rule: has(self.accountRef.name) && size(self.accountRef.name) > 0
            - message: exactly one application variant is required
              rule: (has(self.selfHosted)?1:0)+(has(self.ssh)?1:0)+(has(self.vnc)?1:0)+(has(self.rdp)?1:0)+(has(self.mcp)?1:0)+(has(self.proxyEndpoint)?1:0)
                == 1
            - message: selfHosted is required if and only if type is SelfHosted
              rule: (self.type == 'SelfHosted') == has(self.selfHosted)
            - message: ssh is required if and only if type is SSH
              rule: (self.type == 'SSH') == has(self.ssh)
            - message: vnc is required if and only if type is VNC
              rule: (self.type == 'VNC') == has(self.vnc)
            - message: rdp is required if and only if type is RDP
              rule: (self.type == 'RDP') == has(self.rdp)
            - message: mcp is required if and only if type is MCP
              rule: (self.type == 'MCP') == has(self.mcp)
            - message: proxyEndpoint is required if and only if type is ProxyEndpoint
              rule: (self.type == 'ProxyEndpoint') == has(self.proxyEndpoint)
            - message: ProxyEndpoint requires targetRefs only; other types require
                targetRefs or destinations
              rule: 'self.type == ''ProxyEndpoint'' ? (has(self.targetRefs) && size(self.targetRefs)
                > 0 && (!has(self.destinations) || size(self.destinations) == 0))
                : ((has(self.targetRefs) && size(self.targetRefs) > 0) || (has(self.destinations)
                && size(self.destinations) > 0))'
            - message: targetRefs must reference gateway.networking.k8s.io Gateway
                or HTTPRoute objects
              rule: '!has(self.targetRefs) || self.targetRefs.all(t, t.group == ''gateway.networking.k8s.io''
                && (t.kind == ''Gateway'' || t.kind == ''HTTPRoute''))'
            - message: public destinations must be compiled from targetRefs
              rule: '!has(self.destinations) || self.destinations.all(d, d.type !=
                ''Public'')'
            - message: pathScope requires targetRefs
              rule: '!has(self.pathScope) || (has(self.targetRefs) && size(self.targetRefs)
                > 0)'
            - message: ObserveOnly requires externalRef
              rule: self.managementPolicy != 'ObserveOnly' || has(self.externalRef)
            - message: AdoptById requires externalRef
              rule: self.adoption.mode != 'AdoptById' || has(self.externalRef)
            - message: ObserveOnly requires externalRef for every bypass child
              rule: self.managementPolicy != 'ObserveOnly' || !has(self.bypass) ||
                !has(self.bypass.children) || self.bypass.children.all(c, has(c.externalRef))
          status:
            description: AccessApplicationStatus records the remote application without
              exposing its AUD.
            properties:
              ancestors:
                items:
                  description: |-
                    PolicyAncestorStatus describes the status of a route with respect to an
                    associated Ancestor.

                    Ancestors refer to objects that are either the Target of a policy or above it
                    in terms of object hierarchy. For example, if a policy targets a Service, the
                    Policy's Ancestors are, in order, the Service, the HTTPRoute, the Gateway, and
                    the GatewayClass. Almost always, in this hierarchy, the Gateway will be the most
                    useful object to place Policy status on, so we recommend that implementations
                    SHOULD use Gateway as the PolicyAncestorStatus object unless the designers
                    have a _very_ good reason otherwise.

                    In the context of policy attachment, the Ancestor is used to distinguish which
                    resource results in a distinct application of this policy. For example, if a policy
                    targets a Service, it may have a distinct result per attached Gateway.

                    Policies targeting the same resource may have different effects depending on the
                    ancestors of those resources. For example, different Gateways targeting the same
                    Service may have different capabilities, especially if they have different underlying
                    implementations.

                    For example, in BackendTLSPolicy, the Policy attaches to a Service that is
                    used as a backend in a HTTPRoute that is itself attached to a Gateway.
                    In this case, the relevant object for status is the Gateway, and that is the
                    ancestor object referred to in this status.

                    Note that a parent is also an ancestor, so for objects where the parent is the
                    relevant object for status, this struct SHOULD still be used.

                    This struct is intended to be used in a slice that's effectively a map,
                    with a composite key made up of the AncestorRef and the ControllerName.
                  properties:
                    ancestorRef:
                      description: |-
                        AncestorRef corresponds with a ParentRef in the spec that this
                        PolicyAncestorStatus struct describes the status of.
                      properties:
                        group:
                          default: gateway.networking.k8s.io
                          description: |-
                            Group is the group of the referent.
                            When unspecified, "gateway.networking.k8s.io" is inferred.
                            To set the core API group (such as for a "Service" kind referent),
                            Group must be explicitly set to "" (empty string).

                            Support: Core
                          maxLength: 253
                          pattern: ^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                          type: string
                        kind:
                          default: Gateway
                          description: |-
                            Kind is kind of the referent.

                            There are two kinds of parent resources with "Core" support:

                            * Gateway (Gateway conformance profile)
                            * Service (Mesh conformance profile, ClusterIP Services only)

                            Support for other resources is Implementation-Specific.
                          maxLength: 63
                          minLength: 1
                          pattern: ^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$
                          type: string
                        name:
                          description: |-
                            Name is the name of the referent.

                            Support: Core
                          maxLength: 253
                          minLength: 1
                          type: string
                        namespace:
                          description: |-
                            Namespace is the namespace of the referent. When unspecified, this refers
                            to the local namespace of the Route.

                            Note that there are specific rules for ParentRefs which cross namespace
                            boundaries. Cross-namespace references are only valid if they are explicitly
                            allowed by something in the namespace they are referring to. For example:
                            Gateway has the AllowedRoutes field, and ReferenceGrant provides a
                            generic way to enable any other kind of cross-namespace reference.

                            <gateway:experimental:description>
                            ParentRefs from a Route to a Service in the same namespace are "producer"
                            routes, which apply default routing rules to inbound connections from
                            any namespace to the Service.

                            ParentRefs from a Route to a Service in a different namespace are
                            "consumer" routes, and these routing rules are only applied to outbound
                            connections originating from the same namespace as the Route, for which
                            the intended destination of the connections are a Service targeted as a
                            ParentRef of the Route.
                            </gateway:experimental:description>

                            Support: Core
                          maxLength: 63
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        port:
                          description: |-
                            Port is the network port this Route targets. It can be interpreted
                            differently based on the type of parent resource.

                            When the parent resource is a Gateway, this targets all listeners
                            listening on the specified port that also support this kind of Route(and
                            select this Route). It's not recommended to set `Port` unless the
                            networking behaviors specified in a Route must apply to a specific port
                            as opposed to a listener(s) whose port(s) may be changed. When both Port
                            and SectionName are specified, the name and port of the selected listener
                            must match both specified values.

                            <gateway:experimental:description>
                            When the parent resource is a Service, this targets a specific port in the
                            Service spec. When both Port (experimental) and SectionName are specified,
                            the name and port of the selected port must match both specified values.
                            </gateway:experimental:description>

                            Implementations MAY choose to support other parent resources.
                            Implementations supporting other types of parent resources MUST clearly
                            document how/if Port is interpreted.

                            For the purpose of status, an attachment is considered successful as
                            long as the parent resource accepts it partially. For example, Gateway
                            listeners can restrict which Routes can attach to them by Route kind,
                            namespace, or hostname. If 1 of 2 Gateway listeners accept attachment
                            from the referencing Route, the Route MUST be considered successfully
                            attached. If no Gateway listeners accept attachment from this Route,
                            the Route MUST be considered detached from the Gateway.

                            Support: Extended
                          format: int32
                          maximum: 65535
                          minimum: 1
                          type: integer
                        sectionName:
                          description: |-
                            SectionName is the name of a section within the target resource. In the
                            following resources, SectionName is interpreted as the following:

                            * Gateway: Listener name. When both Port (experimental) and SectionName
                            are specified, the name and port of the selected listener must match
                            both specified values.
                            * Service: Port name. When both Port (experimental) and SectionName
                            are specified, the name and port of the selected listener must match
                            both specified values.

                            Implementations MAY choose to support attaching Routes to other resources.
                            If that is the case, they MUST clearly document how SectionName is
                            interpreted.

                            When unspecified (empty string), this will reference the entire resource.
                            For the purpose of status, an attachment is considered successful if at
                            least one section in the parent resource accepts it. For example, Gateway
                            listeners can restrict which Routes can attach to them by Route kind,
                            namespace, or hostname. If 1 of 2 Gateway listeners accept attachment from
                            the referencing Route, the Route MUST be considered successfully
                            attached. If no Gateway listeners accept attachment from this Route, the
                            Route MUST be considered detached from the Gateway.

                            Support: Core
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                          type: string
                      required:
                      - name
                      type: object
                    conditions:
                      description: |-
                        Conditions describes the status of the Policy with respect to the given Ancestor.

                        <gateway:util:excludeFromCRD>

                        Notes for implementors:

                        Conditions are a listType `map`, which means that they function like a
                        map with a key of the `type` field _in the k8s apiserver_.

                        This means that implementations must obey some rules when updating this
                        section.

                        * Implementations MUST perform a read-modify-write cycle on this field
                          before modifying it. That is, when modifying this field, implementations
                          must be confident they have fetched the most recent version of this field,
                          and ensure that changes they make are on that recent version.
                        * Implementations MUST NOT remove or reorder Conditions that they are not
                          directly responsible for. For example, if an implementation sees a Condition
                          with type `special.io/SomeField`, it MUST NOT remove, change or update that
                          Condition.
                        * Implementations MUST always _merge_ changes into Conditions of the same Type,
                          rather than creating more than one Condition of the same Type.
                        * Implementations MUST always update the `observedGeneration` field of the
                          Condition to the `metadata.generation` of the Gateway at the time of update creation.
                        * If the `observedGeneration` of a Condition is _greater than_ the value the
                          implementation knows about, then it MUST NOT perform the update on that Condition,
                          but must wait for a future reconciliation and status update. (The assumption is that
                          the implementation's copy of the object is stale and an update will be re-triggered
                          if relevant.)

                        </gateway:util:excludeFromCRD>
                      items:
                        description: Condition contains details for one aspect of
                          the current state of this API Resource.
                        properties:
                          lastTransitionTime:
                            description: |-
                              lastTransitionTime is the last time the condition transitioned from one status to another.
                              This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                            format: date-time
                            type: string
                          message:
                            description: |-
                              message is a human readable message indicating details about the transition.
                              This may be an empty string.
                            maxLength: 32768
                            type: string
                          observedGeneration:
                            description: |-
                              observedGeneration represents the .metadata.generation that the condition was set based upon.
                              For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                              with respect to the current state of the instance.
                            format: int64
                            minimum: 0
                            type: integer
                          reason:
                            description: |-
                              reason contains a programmatic identifier indicating the reason for the condition's last transition.
                              Producers of specific condition types may define expected values and meanings for this field,
                              and whether the values are considered a guaranteed API.
                              The value should be a CamelCase string.
                              This field may not be empty.
                            maxLength: 1024
                            minLength: 1
                            pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                            type: string
                          status:
                            description: status of the condition, one of True, False,
                              Unknown.
                            enum:
                            - "True"
                            - "False"
                            - Unknown
                            type: string
                          type:
                            description: type of condition in CamelCase or in foo.example.com/CamelCase.
                            maxLength: 316
                            pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                            type: string
                        required:
                        - lastTransitionTime
                        - message
                        - reason
                        - status
                        - type
                        type: object
                      maxItems: 8
                      minItems: 1
                      type: array
                      x-kubernetes-list-map-keys:
                      - type
                      x-kubernetes-list-type: map
                    controllerName:
                      description: |-
                        ControllerName is a domain/path string that indicates the name of the
                        controller that wrote this status. This corresponds with the
                        controllerName field on GatewayClass.

                        Example: "example.net/gateway-controller".

                        The format of this field is DOMAIN "/" PATH, where DOMAIN and PATH are
                        valid Kubernetes names
                        (https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names).

                        Controllers MUST populate this field when writing status. Controllers should ensure that
                        entries to status populated with their ControllerName are cleaned up when they are no
                        longer necessary.
                      maxLength: 253
                      minLength: 1
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*\/[A-Za-z0-9\/\-._~%!$&'()*+,;=:]+$
                      type: string
                  required:
                  - ancestorRef
                  - conditions
                  - controllerName
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              applicationId:
                type: string
              appliedAt:
                description: AppliedAt is when AppliedHash was last recorded; nil
                  means never applied.
                format: date-time
                type: string
              appliedHash:
                description: AppliedHash is the desired-state hash recorded after
                  the last successful remote convergence.
                type: string
              bypassApplications:
                items:
                  description: AccessBypassApplicationStatus records an operator-owned
                    public carve-out application.
                  properties:
                    applicationId:
                      type: string
                    deletionPolicy:
                      description: DeletionPolicy controls whether a managed remote
                        resource is deleted or orphaned.
                      enum:
                      - Delete
                      - Orphan
                      type: string
                    hostname:
                      type: string
                    name:
                      type: string
                    origin:
                      description: AccessBypassApplicationOrigin records how a bypass
                        child entered management.
                      enum:
                      - Created
                      - Recovered
                      - Adopted
                      type: string
                    path:
                      type: string
                    policyId:
                      type: string
                  required:
                  - applicationId
                  - hostname
                  - path
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - hostname
                - path
                x-kubernetes-list-type: map
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              dataPlanes:
                items:
                  description: AccessApplicationDataPlaneStatus records the selected
                    tunnel and Envoy domain.
                  properties:
                    envoyPort:
                      format: int32
                      type: integer
                    listener:
                      description: |-
                        SectionName is the name of a section in a Kubernetes resource.

                        In the following resources, SectionName is interpreted as the following:

                        * Gateway: Listener name
                        * HTTPRoute: HTTPRouteRule name
                        * Service: Port name

                        Section names can have a variety of forms, including RFC 1123 subdomains,
                        RFC 1123 labels, or RFC 1035 labels.

                        This validation is based off of the corresponding Kubernetes validation:
                        https://github.com/kubernetes/apimachinery/blob/02cfb53916346d085a6c6c7c66f882e3c6b0eca6/pkg/util/validation/validation.go#L208

                        Valid values include:

                        * "example"
                        * "foo-example"
                        * "example.com"
                        * "foo.example.com"

                        Invalid values include:

                        * "example.com/bar" - "/" is an invalid character
                      maxLength: 253
                      minLength: 1
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                      type: string
                    protectionDomain:
                      type: string
                    tunnel:
                      type: string
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              destinations:
                items:
                  description: AccessApplicationDestinationStatus records a compiled
                    Access destination.
                  properties:
                    cidr:
                      type: string
                    hostname:
                      type: string
                    l4Protocol:
                      description: AccessL4Protocol selects a private destination
                        transport protocol. Omission matches both.
                      enum:
                      - TCP
                      - UDP
                      type: string
                    mcpServerId:
                      type: string
                    portRange:
                      type: string
                    type:
                      description: AccessApplicationDestinationType selects a Cloudflare
                        Access destination variant.
                      enum:
                      - Public
                      - Private
                      - ViaMCPServerPortal
                      - Worker
                      - PreviewWorker
                      - AllWorkers
                      - AllPreviewWorkers
                      type: string
                    uri:
                      type: string
                    vnetId:
                      type: string
                    workerId:
                      type: string
                  required:
                  - type
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              domain:
                type: string
              observedGeneration:
                format: int64
                type: integer
              ownershipVerified:
                type: boolean
              tags:
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              type:
                description: AccessApplicationType selects a Gateway-attached Access
                  application variant.
                enum:
                - SelfHosted
                - SSH
                - VNC
                - RDP
                - MCP
                - ProxyEndpoint
                type: string
              zoneId:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
