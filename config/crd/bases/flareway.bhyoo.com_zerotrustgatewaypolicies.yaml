---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: zerotrustgatewaypolicies.flareway.bhyoo.com
spec:
  group: flareway.bhyoo.com
  names:
    categories:
    - flareway
    kind: ZeroTrustGatewayPolicy
    listKind: ZeroTrustGatewayPolicyList
    plural: zerotrustgatewaypolicies
    shortNames:
    - ztgp
    singular: zerotrustgatewaypolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.filters[0]
      name: Filter
      type: string
    - jsonPath: .spec.action
      name: Action
      type: string
    - jsonPath: .status.ruleId
      name: Rule
      type: string
    - jsonPath: .status.conditions[?(@.type=="Accepted")].status
      name: Accepted
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ZeroTrustGatewayPolicy manages one Cloudflare Gateway l4 or dns
          rule.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ZeroTrustGatewayPolicySpec defines one l4 or dns Gateway
              rule.
            properties:
              accountRef:
                description: |-
                  LocalObjectReference contains enough information to let you locate the
                  referenced object inside the same namespace.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              action:
                description: ZeroTrustGatewayAction is an action accepted by Cloudflare
                  Gateway rules.
                enum:
                - "on"
                - "off"
                - allow
                - block
                - scan
                - noscan
                - safesearch
                - ytrestricted
                - isolate
                - noisolate
                - override
                - l4_override
                - audit_ssh
                - egress
                - resolve
                - quarantine
                - redirect
                type: string
              adoption:
                default: {}
                description: AdoptionSpec configures explicit adoption of an existing
                  remote object.
                properties:
                  expect:
                    description: AdoptionExpect declares remote attributes that must
                      match before adoption.
                    properties:
                      domain:
                        type: string
                      name:
                        type: string
                    type: object
                  mode:
                    default: None
                    description: AdoptionMode controls explicit adoption of a remote
                      resource.
                    enum:
                    - None
                    - AdoptById
                    type: string
                type: object
              deletionPolicy:
                default: Orphan
                description: DeletionPolicy controls whether a managed remote resource
                  is deleted or orphaned.
                enum:
                - Delete
                - Orphan
                type: string
              description:
                type: string
              devicePosture:
                type: string
              enabled:
                type: boolean
              externalRef:
                description: ZeroTrustGatewayPolicyExternalReference identifies an
                  existing Gateway rule.
                properties:
                  ruleId:
                    minLength: 1
                    type: string
                required:
                - ruleId
                type: object
              filters:
                items:
                  description: ZeroTrustGatewayFilter selects the Gateway policy evaluation
                    layer.
                  enum:
                  - l4
                  - dns
                  type: string
                maxItems: 1
                minItems: 1
                type: array
                x-kubernetes-list-type: set
              identity:
                type: string
              listRefs:
                items:
                  description: ZeroTrustGatewayListReference identifies a ZeroTrustList
                    in the same namespace.
                  properties:
                    name:
                      minLength: 1
                      type: string
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              managementPolicy:
                default: ObserveOnly
                description: ManagementPolicy controls whether Flareway mutates a
                  remote resource.
                enum:
                - Managed
                - ObserveOnly
                type: string
              name:
                minLength: 1
                type: string
              precedence:
                format: int64
                minimum: 0
                type: integer
              ruleSettings:
                description: ZeroTrustGatewayRuleSettings contains settings supported
                  by l4 and dns rules.
                properties:
                  auditSsh:
                    description: ZeroTrustGatewayAuditSSH configures SSH command logging.
                    properties:
                      commandLogging:
                        type: boolean
                    type: object
                  blockPageEnabled:
                    type: boolean
                  blockReason:
                    type: string
                  checkSession:
                    description: ZeroTrustGatewayCheckSession configures session freshness
                      for an allow rule.
                    properties:
                      duration:
                        type: string
                      enforce:
                        type: boolean
                    type: object
                  ignoreCnameCategoryMatches:
                    type: boolean
                  insecureDisableDnssecValidation:
                    type: boolean
                  ipCategories:
                    type: boolean
                  ipIndicatorFeeds:
                    type: boolean
                  l4Override:
                    description: ZeroTrustGatewayL4Override redirects matching traffic
                      to an address and port.
                    properties:
                      ip:
                        minLength: 1
                        type: string
                      port:
                        format: int32
                        maximum: 65535
                        minimum: 1
                        type: integer
                    required:
                    - ip
                    - port
                    type: object
                  notification:
                    description: ZeroTrustGatewayNotification configures a client
                      notification for blocked traffic.
                    properties:
                      enabled:
                        type: boolean
                      includeContext:
                        type: boolean
                      message:
                        type: string
                      supportUrl:
                        type: string
                    type: object
                  overrideHost:
                    type: string
                  overrideIps:
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                type: object
              traffic:
                minLength: 1
                type: string
            required:
            - accountRef
            - action
            - filters
            - name
            - traffic
            type: object
            x-kubernetes-validations:
            - message: override action requires the dns filter
              rule: self.filters[0] == 'dns' || self.action != 'override'
            - message: l4_override and audit_ssh actions require the l4 filter
              rule: self.filters[0] == 'l4' || !(self.action in ['l4_override', 'audit_ssh'])
            - message: AdoptById requires externalRef
              rule: self.adoption.mode != 'AdoptById' || has(self.externalRef)
            - message: Managed externalRef requires adoption.mode AdoptById
              rule: '!has(self.externalRef) || self.managementPolicy == ''ObserveOnly''
                || self.adoption.mode == ''AdoptById'''
          status:
            description: ZeroTrustGatewayPolicyStatus records the remote rule identity
              and normalized expressions.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observed:
                description: ZeroTrustGatewayPolicyDesiredState is the non-secret
                  state sent to Cloudflare.
                properties:
                  action:
                    description: ZeroTrustGatewayAction is an action accepted by Cloudflare
                      Gateway rules.
                    enum:
                    - "on"
                    - "off"
                    - allow
                    - block
                    - scan
                    - noscan
                    - safesearch
                    - ytrestricted
                    - isolate
                    - noisolate
                    - override
                    - l4_override
                    - audit_ssh
                    - egress
                    - resolve
                    - quarantine
                    - redirect
                    type: string
                  description:
                    type: string
                  devicePosture:
                    type: string
                  enabled:
                    type: boolean
                  filters:
                    items:
                      description: ZeroTrustGatewayFilter selects the Gateway policy
                        evaluation layer.
                      enum:
                      - l4
                      - dns
                      type: string
                    type: array
                  identity:
                    type: string
                  name:
                    type: string
                  precedence:
                    format: int64
                    type: integer
                  ruleSettings:
                    description: ZeroTrustGatewayRuleSettings contains settings supported
                      by l4 and dns rules.
                    properties:
                      auditSsh:
                        description: ZeroTrustGatewayAuditSSH configures SSH command
                          logging.
                        properties:
                          commandLogging:
                            type: boolean
                        type: object
                      blockPageEnabled:
                        type: boolean
                      blockReason:
                        type: string
                      checkSession:
                        description: ZeroTrustGatewayCheckSession configures session
                          freshness for an allow rule.
                        properties:
                          duration:
                            type: string
                          enforce:
                            type: boolean
                        type: object
                      ignoreCnameCategoryMatches:
                        type: boolean
                      insecureDisableDnssecValidation:
                        type: boolean
                      ipCategories:
                        type: boolean
                      ipIndicatorFeeds:
                        type: boolean
                      l4Override:
                        description: ZeroTrustGatewayL4Override redirects matching
                          traffic to an address and port.
                        properties:
                          ip:
                            minLength: 1
                            type: string
                          port:
                            format: int32
                            maximum: 65535
                            minimum: 1
                            type: integer
                        required:
                        - ip
                        - port
                        type: object
                      notification:
                        description: ZeroTrustGatewayNotification configures a client
                          notification for blocked traffic.
                        properties:
                          enabled:
                            type: boolean
                          includeContext:
                            type: boolean
                          message:
                            type: string
                          supportUrl:
                            type: string
                        type: object
                      overrideHost:
                        type: string
                      overrideIps:
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                    type: object
                  traffic:
                    type: string
                required:
                - action
                - filters
                - name
                - traffic
                type: object
              observedGeneration:
                format: int64
                type: integer
              ownershipVerified:
                type: boolean
              ruleId:
                type: string
              wouldApply:
                description: ZeroTrustGatewayPolicyDesiredState is the non-secret
                  state sent to Cloudflare.
                properties:
                  action:
                    description: ZeroTrustGatewayAction is an action accepted by Cloudflare
                      Gateway rules.
                    enum:
                    - "on"
                    - "off"
                    - allow
                    - block
                    - scan
                    - noscan
                    - safesearch
                    - ytrestricted
                    - isolate
                    - noisolate
                    - override
                    - l4_override
                    - audit_ssh
                    - egress
                    - resolve
                    - quarantine
                    - redirect
                    type: string
                  description:
                    type: string
                  devicePosture:
                    type: string
                  enabled:
                    type: boolean
                  filters:
                    items:
                      description: ZeroTrustGatewayFilter selects the Gateway policy
                        evaluation layer.
                      enum:
                      - l4
                      - dns
                      type: string
                    type: array
                  identity:
                    type: string
                  name:
                    type: string
                  precedence:
                    format: int64
                    type: integer
                  ruleSettings:
                    description: ZeroTrustGatewayRuleSettings contains settings supported
                      by l4 and dns rules.
                    properties:
                      auditSsh:
                        description: ZeroTrustGatewayAuditSSH configures SSH command
                          logging.
                        properties:
                          commandLogging:
                            type: boolean
                        type: object
                      blockPageEnabled:
                        type: boolean
                      blockReason:
                        type: string
                      checkSession:
                        description: ZeroTrustGatewayCheckSession configures session
                          freshness for an allow rule.
                        properties:
                          duration:
                            type: string
                          enforce:
                            type: boolean
                        type: object
                      ignoreCnameCategoryMatches:
                        type: boolean
                      insecureDisableDnssecValidation:
                        type: boolean
                      ipCategories:
                        type: boolean
                      ipIndicatorFeeds:
                        type: boolean
                      l4Override:
                        description: ZeroTrustGatewayL4Override redirects matching
                          traffic to an address and port.
                        properties:
                          ip:
                            minLength: 1
                            type: string
                          port:
                            format: int32
                            maximum: 65535
                            minimum: 1
                            type: integer
                        required:
                        - ip
                        - port
                        type: object
                      notification:
                        description: ZeroTrustGatewayNotification configures a client
                          notification for blocked traffic.
                        properties:
                          enabled:
                            type: boolean
                          includeContext:
                            type: boolean
                          message:
                            type: string
                          supportUrl:
                            type: string
                        type: object
                      overrideHost:
                        type: string
                      overrideIps:
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                    type: object
                  traffic:
                    type: string
                required:
                - action
                - filters
                - name
                - traffic
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
