---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: devicesettings.flareway.bhyoo.com
spec:
  group: flareway.bhyoo.com
  names:
    categories:
    - flareway
    kind: DeviceSettings
    listKind: DeviceSettingsList
    plural: devicesettings
    shortNames:
    - dsettings
    singular: devicesettings
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Accepted")].status
      name: Accepted
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DeviceSettings manages the account-wide Cloudflare device settings
          singleton.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DeviceSettingsSpec defines the singleton account device settings.
            properties:
              accountRef:
                description: |-
                  LocalObjectReference contains enough information to let you locate the
                  referenced object inside the same namespace.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              deletionPolicy:
                default: Orphan
                description: DeletionPolicy controls whether a managed remote resource
                  is deleted or orphaned.
                enum:
                - Delete
                - Orphan
                type: string
              disableForTime:
                format: int64
                minimum: 0
                type: integer
              gatewayProxyEnabled:
                type: boolean
              gatewayUdpProxyEnabled:
                type: boolean
              managementPolicy:
                default: ObserveOnly
                description: ManagementPolicy controls whether Flareway mutates a
                  remote resource.
                enum:
                - Managed
                - ObserveOnly
                type: string
              rootCertificateInstallationEnabled:
                type: boolean
              useZtVirtualIp:
                type: boolean
            required:
            - accountRef
            type: object
          status:
            description: DeviceSettingsStatus records observed values and the pending
              ObserveOnly diff.
            properties:
              appliedAt:
                description: AppliedAt is when AppliedHash was last recorded; nil
                  means never applied.
                format: date-time
                type: string
              appliedHash:
                description: AppliedHash is the desired-state hash recorded after
                  the last successful remote convergence.
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observed:
                description: |-
                  DeviceSettingsValues contains account-wide WARP settings. Pointers preserve
                  omitted fields separately from explicit false and zero values.
                properties:
                  disableForTime:
                    format: int64
                    minimum: 0
                    type: integer
                  gatewayProxyEnabled:
                    type: boolean
                  gatewayUdpProxyEnabled:
                    type: boolean
                  rootCertificateInstallationEnabled:
                    type: boolean
                  useZtVirtualIp:
                    type: boolean
                type: object
              observedGeneration:
                format: int64
                type: integer
              wouldApply:
                description: |-
                  DeviceSettingsValues contains account-wide WARP settings. Pointers preserve
                  omitted fields separately from explicit false and zero values.
                properties:
                  disableForTime:
                    format: int64
                    minimum: 0
                    type: integer
                  gatewayProxyEnabled:
                    type: boolean
                  gatewayUdpProxyEnabled:
                    type: boolean
                  rootCertificateInstallationEnabled:
                    type: boolean
                  useZtVirtualIp:
                    type: boolean
                type: object
            type: object
        type: object
        x-kubernetes-validations:
        - message: DeviceSettings must be named default
          rule: self.metadata.name == 'default'
    served: true
    storage: true
    subresources:
      status: {}
