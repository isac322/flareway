---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: cloudflareaccounts.flareway.bhyoo.com
spec:
  group: flareway.bhyoo.com
  names:
    categories:
    - flareway
    kind: CloudflareAccount
    listKind: CloudflareAccountList
    plural: cloudflareaccounts
    shortNames:
    - cfa
    singular: cloudflareaccount
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Accepted")].status
      name: Accepted
      type: string
    - jsonPath: .status.conditions[?(@.type=="CredentialsValid")].status
      name: Credentials
      type: string
    - jsonPath: .status.verified.accountName
      name: Account
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CloudflareAccount defines a Cloudflare account and tenant authorization
          boundary.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CloudflareAccountSpec defines credentials and tenant authorization
              for one account.
            properties:
              accountId:
                description: AccountID is the Cloudflare account identifier.
                pattern: ^[0-9a-f]{32}$
                type: string
              credentials:
                description: Credentials references the API token used for this account.
                properties:
                  apiTokenSecretRef:
                    description: APITokenSecretRef identifies the Cloudflare API token.
                    properties:
                      key:
                        default: token
                        description: Key is the key containing the API token.
                        minLength: 1
                        type: string
                      name:
                        description: Name is the Secret name.
                        minLength: 1
                        type: string
                      namespace:
                        description: Namespace is the Secret namespace.
                        minLength: 1
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                required:
                - apiTokenSecretRef
                type: object
              grants:
                description: Grants define tenant authorization. A namespace matching
                  no grant is denied.
                items:
                  description: CloudflareAccountGrant authorizes one set of tenant
                    operations.
                  properties:
                    accessPolicyRefs:
                      default: Denied
                      description: AccessPolicyRefs permits references to platform
                        Access policy objects.
                      enum:
                      - Allowed
                      - Denied
                      type: string
                    backends:
                      description: Backends configures the account-level backend SSRF
                        boundary.
                      properties:
                        kinds:
                          description: Kinds lists permitted backend kinds.
                          items:
                            description: BackendKind is a Kubernetes backend kind
                              permitted by an account grant.
                            enum:
                            - Service
                            type: string
                          minItems: 1
                          type: array
                          x-kubernetes-list-type: set
                        namespaces:
                          default: Same
                          description: Namespaces selects same-namespace backends
                            or namespaces matching Selector.
                          enum:
                          - Same
                          - Selector
                          type: string
                        selector:
                          description: Selector selects backend namespaces when Namespaces
                            is Selector.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                      required:
                      - kinds
                      type: object
                      x-kubernetes-validations:
                      - message: selector is required when namespaces is Selector
                        rule: self.namespaces != 'Selector' || has(self.selector)
                    exposures:
                      description: Exposures lists permitted listener exposures.
                      items:
                        description: Exposure selects a Cloudflare public-edge or
                          private-WARP exposure.
                        enum:
                        - Public
                        - Private
                        type: string
                      minItems: 1
                      type: array
                      x-kubernetes-list-type: set
                    hostnames:
                      description: Hostnames contains exact hostnames, single-label
                        wildcard hostnames, or "*".
                      items:
                        type: string
                      minItems: 1
                      type: array
                      x-kubernetes-list-type: set
                    namespaceSelector:
                      description: NamespaceSelector selects namespaces governed by
                        this grant.
                      properties:
                        matchExpressions:
                          description: matchExpressions is a list of label selector
                            requirements. The requirements are ANDed.
                          items:
                            description: |-
                              A label selector requirement is a selector that contains values, a key, and an operator that
                              relates the key and values.
                            properties:
                              key:
                                description: key is the label key that the selector
                                  applies to.
                                type: string
                              operator:
                                description: |-
                                  operator represents a key's relationship to a set of values.
                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                type: string
                              values:
                                description: |-
                                  values is an array of string values. If the operator is In or NotIn,
                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                  the values array must be empty. This array is replaced during a strategic
                                  merge patch.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - key
                            - operator
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: |-
                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                          type: object
                      type: object
                      x-kubernetes-map-type: atomic
                    platformObjects:
                      default: Denied
                      description: PlatformObjects permits creation or management
                        of platform-scoped objects.
                      enum:
                      - Allowed
                      - Denied
                      type: string
                    privateRoutes:
                      description: PrivateRoutes selects platform private-route objects
                        this namespace may reference.
                      properties:
                        hostnameRouteSelector:
                          description: HostnameRouteSelector selects permitted HostnameRoute
                            objects. An empty selector matches all.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        networkRouteSelector:
                          description: NetworkRouteSelector selects permitted NetworkRoute
                            objects. An empty selector matches all.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                    unprotectedHostnames:
                      description: UnprotectedHostnames lists hostnames allowed without
                        Cloudflare Access.
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: set
                    zones:
                      description: Zones contains exact zone names or "*".
                      items:
                        type: string
                      minItems: 1
                      type: array
                      x-kubernetes-list-type: set
                  required:
                  - exposures
                  - hostnames
                  - namespaceSelector
                  - zones
                  type: object
                type: array
                x-kubernetes-list-type: atomic
            required:
            - accountId
            - credentials
            type: object
          status:
            description: CloudflareAccountStatus defines observed account verification
              state.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              verified:
                description: CloudflareAccountVerifiedStatus records non-secret account
                  information returned by Cloudflare.
                properties:
                  accountName:
                    type: string
                  authDomain:
                    type: string
                  teamName:
                    type: string
                  tokenPermissions:
                    description: TokenPermissions never contains token values or request
                      bodies.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  zones:
                    items:
                      description: CloudflareVerifiedZone is a zone observed while
                        verifying account credentials.
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                      required:
                      - id
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - id
                    x-kubernetes-list-type: map
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
