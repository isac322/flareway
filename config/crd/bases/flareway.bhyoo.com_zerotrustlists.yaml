---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: zerotrustlists.flareway.bhyoo.com
spec:
  group: flareway.bhyoo.com
  names:
    categories:
    - flareway
    kind: ZeroTrustList
    listKind: ZeroTrustListList
    plural: zerotrustlists
    shortNames:
    - ztlist
    singular: zerotrustlist
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.listId
      name: List
      type: string
    - jsonPath: .status.conditions[?(@.type=="Accepted")].status
      name: Accepted
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ZeroTrustList manages one Cloudflare Gateway list and all of
          its items.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ZeroTrustListSpec defines a whole-object Cloudflare Gateway
              list.
            properties:
              accountRef:
                description: |-
                  LocalObjectReference contains enough information to let you locate the
                  referenced object inside the same namespace.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              adoption:
                default: {}
                description: AdoptionSpec configures explicit adoption of an existing
                  remote object.
                properties:
                  expect:
                    description: AdoptionExpect declares remote attributes that must
                      match before adoption.
                    properties:
                      domain:
                        maxLength: 253
                        type: string
                      name:
                        maxLength: 100
                        type: string
                    type: object
                  mode:
                    default: None
                    description: AdoptionMode controls explicit adoption of a remote
                      resource.
                    enum:
                    - None
                    - AdoptById
                    type: string
                type: object
              deletionPolicy:
                default: Orphan
                description: DeletionPolicy controls whether a managed remote resource
                  is deleted or orphaned.
                enum:
                - Delete
                - Orphan
                type: string
              externalRef:
                description: ZeroTrustListExternalReference identifies an existing
                  Cloudflare Gateway list.
                properties:
                  listId:
                    minLength: 1
                    type: string
                required:
                - listId
                type: object
              items:
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              managementPolicy:
                default: ObserveOnly
                description: ManagementPolicy controls whether Flareway mutates a
                  remote resource.
                enum:
                - Managed
                - ObserveOnly
                type: string
              name:
                minLength: 1
                type: string
              type:
                description: ZeroTrustListType selects the Cloudflare Gateway list
                  item parser.
                enum:
                - SERIAL
                - URL
                - DOMAIN
                - EMAIL
                - IP
                type: string
            required:
            - accountRef
            - name
            - type
            type: object
            x-kubernetes-validations:
            - message: AdoptById requires externalRef
              rule: self.adoption.mode != 'AdoptById' || has(self.externalRef)
            - message: Managed externalRef requires adoption.mode AdoptById
              rule: '!has(self.externalRef) || self.managementPolicy == ''ObserveOnly''
                || self.adoption.mode == ''AdoptById'''
          status:
            description: ZeroTrustListStatus records the remote identity and whole-list
              ownership state.
            properties:
              appliedAt:
                description: AppliedAt is when AppliedHash was last recorded; nil
                  means never applied.
                format: date-time
                type: string
              appliedHash:
                description: AppliedHash is the desired-state hash recorded after
                  the last successful remote convergence.
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              listId:
                type: string
              observed:
                description: ZeroTrustListObservedState records non-secret remote
                  list state.
                properties:
                  items:
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  name:
                    type: string
                  type:
                    description: ZeroTrustListType selects the Cloudflare Gateway
                      list item parser.
                    enum:
                    - SERIAL
                    - URL
                    - DOMAIN
                    - EMAIL
                    - IP
                    type: string
                required:
                - name
                - type
                type: object
              observedGeneration:
                format: int64
                type: integer
              ownershipVerified:
                type: boolean
              wouldApply:
                description: ZeroTrustListItemsDiff is the item-level ObserveOnly
                  diff.
                properties:
                  add:
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  remove:
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
