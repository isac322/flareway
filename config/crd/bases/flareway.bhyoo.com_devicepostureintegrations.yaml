---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: devicepostureintegrations.flareway.bhyoo.com
spec:
  group: flareway.bhyoo.com
  names:
    categories:
    - flareway
    kind: DevicePostureIntegration
    listKind: DevicePostureIntegrationList
    plural: devicepostureintegrations
    shortNames:
    - cfdpi
    singular: devicepostureintegration
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Accepted")].status
      name: Accepted
      type: string
    - jsonPath: .status.integrationId
      name: Integration
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DevicePostureIntegration is a namespaced Cloudflare posture integration.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DevicePostureIntegrationSpec defines a Cloudflare device
              posture integration.
            properties:
              accountRef:
                description: |-
                  LocalObjectReference contains enough information to let you locate the
                  referenced object inside the same namespace.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              adoption:
                default: {}
                description: AdoptionSpec configures explicit adoption of an existing
                  remote object.
                properties:
                  expect:
                    description: AdoptionExpect declares remote attributes that must
                      match before adoption.
                    properties:
                      domain:
                        maxLength: 253
                        type: string
                      name:
                        maxLength: 100
                        type: string
                    type: object
                  mode:
                    default: None
                    description: AdoptionMode controls explicit adoption of a remote
                      resource.
                    enum:
                    - None
                    - AdoptById
                    type: string
                type: object
              config:
                description: |-
                  DevicePostureIntegrationConfig contains provider-specific settings.
                  Secret values are always read from the referenced Kubernetes Secrets.
                properties:
                  accessClientIdRef:
                    description: DevicePostureIntegrationSecretReference identifies
                      one local Secret key.
                    properties:
                      key:
                        minLength: 1
                        type: string
                      name:
                        minLength: 1
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  accessClientSecretRef:
                    description: DevicePostureIntegrationSecretReference identifies
                      one local Secret key.
                    properties:
                      key:
                        minLength: 1
                        type: string
                      name:
                        minLength: 1
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  apiUrl:
                    type: string
                  authUrl:
                    type: string
                  clientId:
                    type: string
                  clientKeyRef:
                    description: DevicePostureIntegrationSecretReference identifies
                      one local Secret key.
                    properties:
                      key:
                        minLength: 1
                        type: string
                      name:
                        minLength: 1
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  clientSecretRef:
                    description: DevicePostureIntegrationSecretReference identifies
                      one local Secret key.
                    properties:
                      key:
                        minLength: 1
                        type: string
                      name:
                        minLength: 1
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  customerId:
                    type: string
                type: object
              deletionPolicy:
                default: Orphan
                description: DeletionPolicy controls whether a managed remote resource
                  is deleted or orphaned.
                enum:
                - Delete
                - Orphan
                type: string
              externalRef:
                description: DevicePostureIntegrationExternalReference identifies
                  an existing integration.
                properties:
                  integrationId:
                    minLength: 1
                    type: string
                required:
                - integrationId
                type: object
              interval:
                minLength: 1
                pattern: ^[1-9][0-9]*[mh]$
                type: string
              managementPolicy:
                default: Managed
                description: ManagementPolicy controls whether Flareway mutates a
                  remote resource.
                enum:
                - Managed
                - ObserveOnly
                type: string
              name:
                minLength: 1
                type: string
              type:
                description: DevicePostureIntegrationType selects a third-party posture
                  provider.
                enum:
                - WorkspaceOne
                - CrowdstrikeS2S
                - Uptycs
                - Intune
                - Kolide
                - TaniumS2S
                - SentinelOneS2S
                - CustomS2S
                type: string
            required:
            - accountRef
            - config
            - interval
            - name
            - type
            type: object
            x-kubernetes-validations:
            - message: ObserveOnly requires externalRef
              rule: self.managementPolicy != 'ObserveOnly' || has(self.externalRef)
            - message: AdoptById requires externalRef
              rule: self.adoption.mode != 'AdoptById' || has(self.externalRef)
            - message: WorkspaceOne requires apiUrl, authUrl, clientId, and clientSecretRef
              rule: self.type != 'WorkspaceOne' || (size(self.config.apiUrl) > 0 &&
                size(self.config.authUrl) > 0 && size(self.config.clientId) > 0 &&
                has(self.config.clientSecretRef))
            - message: CrowdstrikeS2S requires apiUrl, clientId, customerId, and clientSecretRef
              rule: self.type != 'CrowdstrikeS2S' || (size(self.config.apiUrl) > 0
                && size(self.config.clientId) > 0 && size(self.config.customerId)
                > 0 && has(self.config.clientSecretRef))
            - message: Uptycs requires apiUrl, customerId, clientKeyRef, and clientSecretRef
              rule: self.type != 'Uptycs' || (size(self.config.apiUrl) > 0 && size(self.config.customerId)
                > 0 && has(self.config.clientKeyRef) && has(self.config.clientSecretRef))
            - message: Intune requires clientId, customerId, and clientSecretRef
              rule: self.type != 'Intune' || (size(self.config.clientId) > 0 && size(self.config.customerId)
                > 0 && has(self.config.clientSecretRef))
            - message: Kolide requires clientId and clientSecretRef
              rule: self.type != 'Kolide' || (size(self.config.clientId) > 0 && has(self.config.clientSecretRef))
            - message: TaniumS2S requires apiUrl and clientSecretRef; Access credentials
                must be supplied together
              rule: self.type != 'TaniumS2S' || (size(self.config.apiUrl) > 0 && has(self.config.clientSecretRef)
                && (has(self.config.accessClientIdRef) == has(self.config.accessClientSecretRef)))
            - message: SentinelOneS2S requires apiUrl and clientSecretRef
              rule: self.type != 'SentinelOneS2S' || (size(self.config.apiUrl) > 0
                && has(self.config.clientSecretRef))
            - message: CustomS2S requires apiUrl and Access client credential references
              rule: self.type != 'CustomS2S' || (size(self.config.apiUrl) > 0 && has(self.config.accessClientIdRef)
                && has(self.config.accessClientSecretRef))
          status:
            description: DevicePostureIntegrationStatus defines the observed state
              without credentials.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              integrationId:
                type: string
              observed:
                description: DevicePostureIntegrationObservedState retains mutable
                  non-secret remote fields.
                properties:
                  config:
                    description: DevicePostureIntegrationObservedConfig contains only
                      non-secret remote configuration.
                    properties:
                      apiUrl:
                        type: string
                      authUrl:
                        type: string
                      clientId:
                        type: string
                      customerId:
                        type: string
                    type: object
                  interval:
                    type: string
                  name:
                    type: string
                  type:
                    description: DevicePostureIntegrationType selects a third-party
                      posture provider.
                    enum:
                    - WorkspaceOne
                    - CrowdstrikeS2S
                    - Uptycs
                    - Intune
                    - Kolide
                    - TaniumS2S
                    - SentinelOneS2S
                    - CustomS2S
                    type: string
                type: object
              observedGeneration:
                format: int64
                type: integer
              ownershipVerified:
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
